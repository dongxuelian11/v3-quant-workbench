# Current status

Status: **V1.1 LOCAL CANDIDATE / PRE-ALPHA / RESEARCH-ONLY / NOT_FORMAL**

This is the current non-P0 implementation status, not a P0 target architecture or a release announcement. The V1.1 candidate is still an unpushed dirty worktree whose package version remains `1.0.0`.

| Area | Current state | Evidence ceiling / missing proof |
|---|---|---|
| Product Home and Desktop ↔ backend | `PRODUCT_CONNECTED` local candidate through the supervised packaged runtime and closed bridge. Create/open/switch, backend-owned Project Home, truthful staged navigation, Task feedback, restart recovery, and degraded states are wired. | Same-machine local/package evidence only. Hosted exact-head and independent review are `NOT_RUN` / `PENDING`. |
| Local Data | `PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL`. Native chooser plus one-use main-process transfer admits bounded CSV/Parquet into canonical RawCapture/Snapshot/Universe/Artifact owners; the renderer supplies no canonical IDs or authoritative bytes. | `LOCAL_USER_SUPPLIED / PIT_UNPROVABLE`; complete PIT, revision, trading-status, listing, price-limit, and corporate-action truth are unavailable. |
| Live free provider | Product acquisition is fail-closed and performs no fixture/provider fallback. | Exact-package AKShare acceptance is `NOT_RUN`. External unavailability may yield exact `PROVIDER_ACQUISITION_UNAVAILABLE`; it cannot be reported as PASS. |
| Factor Research | `PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL`. The sole canonical evaluator, manifest-aware payload resolution, materialization, Factor Analysis, persistence, and restart readback are wired. | Single-symbol cross-sectional metrics remain `INSUFFICIENT_SAMPLE`; no Formal claim. |
| Research Strategy / Portfolio / Risk | The V1.1 research path publishes a canonical research Strategy and decision-chain artifacts from exact Factor/context refs. | This does not bind the broad Formal `StrategyService`, `PortfolioService`, or `RiskService` product operation sets; they remain unavailable in the advertised ASL capability map. |
| Research Backtest / Result | `PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL`. Product Entry submits an isolated research Backtest, publishes a VALID Result plus Result Analytics/lineage artifacts, exposes tables/charts/exports, and rediscovers exact identities after restart. | VALID is a publication state, not Formal market truth. Legacy Formal `BacktestService` remains `UNAVAILABLE / FORMAL_EXECUTION_CONTRACT_NOT_CLOSED`; broad `ResultService` remains unavailable as a separate ASL facade. |
| Model | Domain modules and bounded accepted backend slices exist. | No V1.1 Product Entry page/operation set proves Model `PRODUCT_CONNECTED`; broad `ModelService` is unavailable in the product capability map. |
| Agent | L0 READ / L1 DRAFT only. | L2 EXECUTE and L3 PUBLISH remain `NOT_AVAILABLE / NOT_RUN` until shared canonical user-action authority exists. |
| Visual/accessibility | 60/60 packaged Product page screenshots and the machine baseline pass across four viewports and Electron zoom 100/125/150, including navigation keyboard activation, focus visibility, labels, textual status, and chart alternatives. | This is `PACKAGED_SAME_MACHINE / EMULATED_ELECTRON_ZOOM`; physical Windows scaling is `NOT_RUN`, native-file-chooser keyboard completion is `NOT_RUN`, and user visual acceptance is `PENDING`. |
| Windows package | Exact current unpacked package verification and V1.1 Journey A/B pass on the same machine with embedded CPython 3.14.5 and AKShare 1.18.84. | BuildManifest is DIRTY, so `verify:release` correctly fails. Installer qualification, clean release identity, and distinct clean-machine proof are `NOT_RUN`. |
| CI / governance | Jobs A-C and D-F are defined at exact candidate/package boundaries; all repository Actions are pinned to audited immutable SHAs and local source/YAML/shell contracts pass. | No remote task branch or PR exists. Hosted Jobs A-F are `NOT_RUN`; independent review is `PENDING`. |
| Version | Repository/package/installer naming remains `1.0.0`. | The plan permits `1.1.0` only after the complete C4 acceptance boundary; that boundary is not yet met. |
| Market scope target | China A-share daily/end-of-day research. | Current local acceptance data does not establish institutional or Formal market truth. |
| Live trading / paper trading | Not included. | `NOT_AVAILABLE`. |
| Production/trading readiness | False. | No `PRODUCTION_AVAILABLE` claim. |
| Project license | Apache-2.0. | Dependency licenses remain separately recorded. |

The current exact evidence matrix and remaining actions are in [`V1_1_USABLE_RESEARCH_PRODUCT_STATE_LEDGER.md`](../release/V1_1_USABLE_RESEARCH_PRODUCT_STATE_LEDGER.md). Contract types or backend modules alone do not promote an operation to `PRODUCT_CONNECTED` or `PRODUCTION_AVAILABLE`.
