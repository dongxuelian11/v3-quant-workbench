# TASK_GOAL

Execute V3 V1.1 Usable Research Product as one bounded program, one branch, one final PR, and four checkpoint commits. A first-time user must be able to complete a truthful A-share research journey without source code or demo substitution. Preserve PRE_ALPHA / RESEARCH_ONLY / NOT_FORMAL truth, exact canonical ownership, the existing dirty root, and compaction-safe progress. After C1-C4, re-audit source and observable product effects against this goal before any push decision.

# TASK_PROGRESS

- current_checkpoint: C4_USABILITY_RELEASE_QUALIFICATION
- checkpoint_state: C4_COMMITTED_AND_PUSHED; FIRST_BOUNDED_CORRECTION_PUSHED; SECOND_BOUNDED_CORRECTION_EXPLICITLY_AUTHORIZED_AND_IN_PROGRESS
- last_completed_action: The second bounded correction is implemented and locally validated across exactly seven tracked files. Focused red-to-green evidence covers same-process Electron installation, full-driver raw-ASAR mode and delayed protocol reap. Adjacent Node tests are 159/159 and Product Research tests are 16/16. One complete elevated D-worktree `validate:public` is PASS: Authority 1.0.2, typecheck/lint, Node 159/159, backend 41/41 suites / 74/74 files / 1081/1081 cases plus compile gate, 35-module build, bundle/build-manifest verification, Electron runtime 121/121, C1 31 Python + 79 Electron, frontend, secret scan, repository audit and 518-entry SBOM. A current local Windows package was built on D with CPython 3.14.5, 52 dependencies, 13,904 files and package verification PASS. Its release verification correctly remained NOT_AVAILABLE because the pre-commit BuildManifest is `DIRTY`. The actual packaged Electron executable then ran the corrected V1.1 driver under `ELECTRON_RUN_AS_NODE=1`: Job E's former ASAR copy point passed; Journey A 3/3 and Journey B 2/2 phases passed; both cold-restart identities and the 1.05 GB install-tree before/after identity are equal; no Electron/backend process remains. This is same-machine candidate evidence only, not hosted clean-machine evidence.
- exact_next_action: Reaudit the seven-file bounded delta, P0 hashes/diff and exact GitHub main/branch/PR #50 CURRENT; stage only the seven recorded files; create one ordinary non-amended second-correction commit; push without force to the same branch. Bind fresh A-F only to the resulting exact remote HEAD, wait to terminal, inspect actual logs/artifacts/source effects, keep PR #50 OPEN / UNMERGED and do not promote provider/user/independent-review truth.
- active_acceptance_id: ACC_C4_SECOND_BOUNDED_CORRECTION_B_C_E_F
- current_dirty_inventory: seven tracked bounded files, zero untracked files; ignored exact-head and generated build/package evidence remains under the D-drive worktree `artifacts/` boundary.
- files_currently_modified:
  - .github/workflows/ci.yml
  - apps/backend/src/v3_backend/runtime/product_workers.py
  - apps/backend/tests/product_runtime/test_product_runtime_research.py
  - docs/release/V1_1_USABLE_RESEARCH_PRODUCT_STATE_LEDGER.md
  - scripts/product-closure-packaged-e2e.mjs
  - scripts/v1_1_product_release_packaged_e2e.mjs
  - tests/unit/v1-1-ci-workflow.test.mjs
- tests:
  - c4_second_bounded_correction_precommit_current: PASS — branch/local/tracking/API remote heads still equal `963e659c42492d15b3531867d53524c782f0893c`; main remains `02c5b8748170569ffc436f3bf5d2f682c21d2811`; PR #50 is OPEN / UNMERGED / BLOCKED at that exact head; its four previous exact-head runs remain the known terminal failures and are not reused. The bounded write set is exactly seven tracked files with zero untracked files, P0 diff is empty, all five recorded hashes still match, Authority 1.0.2 validates and `git diff --check` passes.
  - c4_second_bounded_correction_focused_regressions: PASS — old implementation was first observed RED at workflow assertions 5/7 and delayed-reap worker 0/1. After the minimum implementation, workflow/driver source contracts are 7/7, delayed protocol reap plus real deadline escalation and isolated canonical success are 3/3, both `.mjs` drivers pass syntax checking and `git diff --check` passes. Full and hosted evidence remain NOT_RUN at this checkpoint.
  - c4_second_bounded_correction_full_public: PASS — one uninterrupted `npm.cmd run validate:public` completed under the exact D-worktree write boundary: Authority 1.0.2; Node 159/159; backend 41/41 suites, 74/74 files and 1081/1081 cases plus compile; build/bundle/build-manifest; Electron 121/121; C1 31 Python + 79 Electron; frontend, secret scan, repo audit and 518-entry SBOM. Two earlier sandboxed attempts are recorded only as environment-blocked partial runs: the first stopped at `.pyc` temporary creation after 1081/1081, the second stopped at `dist` unlink after the same backend PASS; neither is promoted to full PASS.
  - c4_second_bounded_correction_same_machine_package: PASS at LOCAL_CANDIDATE / ISOLATED_SAME_MACHINE only — current dirty candidate package is 1,049,995,359 bytes across 13,904 files, embeds CPython 3.14.5 and 52 dependencies, and `verify:package` passes. `verify:release` correctly rejects the pre-commit DIRTY BuildManifest, so release/CLEAN evidence remains NOT_AVAILABLE locally. The package's Electron executable running the exact current V1.1 driver with `ELECTRON_RUN_AS_NODE=1` produces `PASS_CANDIDATE`, LOCAL_USER_SUPPLIED / NOT_FORMAL / PRE_ALPHA / PRODUCT_CONNECTED, Journey A 3/3, Journey B 2/2, cold-restart equality, byte-identical package tree before/after and zero orphan processes. Hosted no-checkout Job E remains NOT_RUN until push.
  - c4_second_bounded_correction_admission: PASS — explicit user authorization is recorded; Authority 1.0.2 hashes match; local/tracking/API branch heads equal `963e659c42492d15b3531867d53524c782f0893c`; tree is `942dcfaa486784426026fa908e6032216bb0fd92`; PR #50 remains OPEN / UNMERGED / BLOCKED. Remote failure evidence is exact-current and no provider request was reached by Job F.
  - c4_exact_head_remote_gate_bounded_correction_local: PASS at LOCAL_WORKTREE scope — exact remote failure logs from runs 32810321139/32810405694 and 32810321181/32810405728 were traced through production source before editing. The corrected five-file delta passes workflow/local-data focus 9/9, isolated worker focus 1/1, Product Runtime 62/62, Electron runtime 121/121, real Electron capture/restart, typecheck/lint/build and the complete `validate:public` inventory (Node 158/158; backend 1080/1080; Electron 121/121; C1 30+79; remaining public gates PASS). Hosted correction-head execution remains NOT_RUN until the ordinary correction commit is pushed.
  - c4_documentation_truth_reconciliation: PASS at LOCAL_WORKTREE scope — source tracing confirmed the additive `ProductEntryService` V1.1 path is PRODUCT_CONNECTED while the legacy Formal `BacktestService` remains exact `UNAVAILABLE / FORMAL_EXECUTION_CONTRACT_NOT_CLOSED`; docs now state both facts. Read-only GitHub CURRENT proved historical V1.0 PR #49 MERGED, exact-main CI 32626717592 SUCCESS, packaging 32626717564 SUCCESS and public `v1.0.0` prerelease. The new docs guard rejects the former Desktop/Backtest “not rebuilt”, V1.0 PENDING and premature package-version claims; `npm.cmd run smoke:frontend` PASS. Hosted/clean-machine/provider/physical/user/review evidence remains unpromoted.
  - c4_final_source_effect_write_set_audit: PASS at LOCAL_CANDIDATE scope — the admitted-base-to-worktree lineage contains 145 tracked paths plus the six current untracked C4 files, all inside the recorded backend owners/contracts/tests, Desktop main/renderer/bridge, release/CI/docs and validation boundaries. P0 lineage diff is empty; all five authority hashes match; generated `artifacts/` evidence is ignored; no generated package is tracked; `git diff --check` PASS; package/package-lock root versions are exactly `1.0.0`; all workflow Actions are immutable audited SHAs and no floating Action ref exists. Actual source inspection confirms Project Home gates navigation from canonical read models, renderer calls the real Product bridge, owner payloads are resolved in backend/Main, and the broad Formal `BacktestService` remains fail-closed rather than being silently reinterpreted by the additive research path.
  - c4_final_validate_public: PASS — current post-documentation `npm.cmd run validate:public` exits 0: authority 1.0.2, typecheck/lint, Node 158/158, backend 41/41 suites / 74/74 discovered files / 1080/1080 plus compile gate, 35-module build and bundle/build-manifest truth, Electron runtime 121/121, C1 fault matrix (30 Python + 79 Electron), current documentation/frontend truth, secret/history scan over 747 current source files, repository audit and 518-entry SBOM all pass. BuildManifest remains correctly DIRTY.
  - c4_unified_ci_clean_machine_red_green: EXPECTED FAIL then SOURCE PASS / HOSTED NOT_RUN — the original contract failed on absent Jobs A-F, floating tags and missing V1.1 wiring. After explicit approval, the strengthened six-case contract passes 6/6 and proves exact-head responsibility split, immutable action SHAs plus release annotations, bounded release-path triggers, transferred-artifact-only Job E drivers, Job F child-environment removal of `ELECTRON_RUN_AS_NODE`, deterministic provider mode and local-source mode, and exact provider-failure classification. Both YAML files and 14/14 explicit PowerShell blocks parse; `smoke:frontend` and the 15/15 fixture/codegen contract pass. Hosted Jobs A-F remain NOT_RUN until an exact remote head exists.
  - c4_experiment_success_output_red_green: EXPECTED FAIL then PASS — exact-current full backend execution found `BacktestService.v1.expandExperiment` persisted list-valued `child_task_ids` inside `TASK_SUCCEEDED.outputs`, while `_task_read_model` correctly accepts only non-empty scalar role/value pairs. The child identities are already canonically owned by `task_dependency` and the content-addressed expansion manifest, so the success event now exposes only `manifest_artifact_id`; the lifecycle regression first asserts an OK TaskService response and rejects a shadow `child_task_ids` output. Exact focus is 1/1 and the complete Product Runtime suite is 62/62 PASS; no read-model validation was weakened.
  - c4_exact_current_validate_public: PASS — after the experiment output correction, the earlier exact-current `npm.cmd run validate:public` exited 0 with Node 157/157, backend 1080/1080, Electron runtime 121/121 and all remaining public gates. The first invocation's post-test compileall failure was a Codex sandbox write denial against the D-drive worktree, not a source assertion; the same command passed unchanged with the exact worktree write permission admitted. The later `c4_final_validate_public` entry supersedes its counts after the documentation guard was added.
  - c4_current_package_effect: PASS_CANDIDATE at PACKAGED_SAME_MACHINE / DIRTY_BUILD_MANIFEST / PRE_ALPHA / NOT_FORMAL — the exact current source rebuilt the Windows x64 package and NSIS installer with explicitly bound CPython 3.14.5. `verify:package` passes for 1,049,994,307 bytes / 13,904 files / SHA-256 `973e8b78c31320f8d26893104922df6c0da11e74bd8e20d8fc5325233db01932`, embedded AKShare 1.18.84, 52 Python dependencies and no first-launch network install. The current V1.1 A/B package smoke passes complete A Data/Factor/Strategy/VALID Result restart identity and B independent sample/IC/RankIC/quantile/spread equality. `verify:release` correctly stops at `release BuildManifest is not CLEAN`; therefore installer production/release qualification, distinct clean machine and hosted proof remain NOT_RUN. The retained detailed Journey evidence is under D-drive `artifacts/v3-v1-1-release-current-final`.
  - c4_product_home_truth_red_green: EXPECTED FAIL then PASS at LOCAL_WORKTREE plus PACKAGED_SAME_MACHINE / EMULATED_ELECTRON_ZOOM scope — the first source contract failed on absent Home navigation/stage/switch responsibilities. The second runtime regression failed with `CANONICAL_RUN_SPEC_REQUIRED` after a verified V1.1 Project Home. Both now pass: Home summaries and next actions derive only from `ProductProjectHomeView`; project open/switch/create remains a real main-owned binding action; legacy B3 Product Research/RunSpec and BuildManifest diagnostics are folded under an explicitly non-Golden compatibility entry. Shell focus is 6/6, T4 surface focus 3/3, the adjacent renderer/bridge/release set is 58/58, typecheck/lint/35-module build and unpacked package build pass. `artifacts/visual-probe-9-home-final` is the exact current-package effect evidence: 60/60 screenshots, machine baseline PASS, zero failures, 12 Home cases with no horizontal scroll/clipping/unnamed control, actual Tab/Return nav, graceful exit and no orphan backend. Physical scaling and user visual acceptance remain NOT_RUN/PENDING.
  - c4_packaged_visual_matrix_final_machine_baseline: PASS at PACKAGED_SAME_MACHINE / EMULATED_ELECTRON_ZOOM scope — `artifacts/visual-probe-6-final` contains 60 non-empty screenshots and exact per-page DOM evidence for 1366×768, 1440×900, 1920×1080 and 2560×1440 at Electron zoom 100/125/150. All layout/font/accessible-name/form-label/status/chart-alternative checks pass; Home/Data/Research/Backtest/Results each pass actual Tab/Return activation and a 3px visible focus outline. The real package exits 0 and gracefully shuts down its bundled backend. This proves a machine visual/accessibility baseline only: `physical_windows_scaling=NOT_RUN`, `user_visual_acceptance=PENDING_USER_REVIEW`, and a keyboard-complete native file chooser Golden Journey is NOT_RUN. Graceful shutdown still logs late renderer read attempts rejected as `BACKEND_DISCONNECTED (SHUTTING_DOWN)` after the smoke evidence is complete; no forced fallback or orphan process occurs, but the log noise remains for final C4 source/effect review.
  - c4_packaged_visual_matrix_probe_3: PARTIAL PASS / FAIL at PACKAGED_SAME_MACHINE scope — `artifacts/visual-probe-3` captured 60/60 non-empty images and the real package exited 0 with `PACKAGED_RUNTIME_SELECTED`, `PRODUCT_CLOSURE_PACKAGED_SMOKE_PASS`, `GRACEFUL_SHUTDOWN_SUCCESS` and zero residual package processes. All 12 emulated zoom cases × five Product pages have no shell overflow, horizontally clipped interaction, missing accessible name/form label, primary text below 13px, important table below 12px, or missing chart alternative after bounded corrections. Electron Tab moves focus in the exact nav order, but the two-event Enter injection did not activate buttons or expose `:focus-visible`; machine baseline remains FAIL. Current source replaces that injection with documented rawKeyDown/char/keyUp Return and records focus before activation; packaged rerun is NOT_RUN. Physical Windows scaling is NOT_RUN and `USER_VISUAL_ACCEPTED=PENDING_USER_REVIEW`.
  - c4_packaged_golden_journeys_final_same_machine: PASS_CANDIDATE at PACKAGED_SAME_MACHINE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL scope — exact root `artifacts/v3-v1-1-release-3LIkjN` and BuildManifest `bmanifest_sha256_aca1317479cd41b019d12e8d45ca659f971d53955ea75fdb781edac23d6930eb` contain four successful phases. Journey A preserves Project `prj_DEHZMZBY8N7EK9TC1FB4QNGAPG`, Context `pcr_H19WMSW8NN4SH2KF4C28NHQTNM`, Data/Factor/Strategy/VALID Backtest Result plus exact Result/Analytics/Lineage identities across process restart. Journey B preserves Project `prj_7S7M1MR3S905Q10PXECKMFYR7M`, Context `pcr_8V3440RM313A331A4R9GPN54YG`, Data/Factor identities and independently matched sample 20, IC 0.9962117698528705, Rank IC 1, quantiles `[0.006290072995816576, 0.009442996696286121, 0.012171514357436464, 0.014555939272421103, 0.01665751760264017]`, spread 0.010367444606823595 before/after restart. Create phases are `FIRST_PROCESS`; reopen phases are `NEW_PROCESS`. Every phase records PACKAGED runtime, LOCAL_USER_SUPPLIED, graceful shutdown, no known-ID injection, terminated backend PID and an unchanged copied install tree. Package identity is 1,049,843,911 bytes / 13,903 files / SHA-256 `447a567606df924aa6f1af5e43a897605a996a483ef01b3fe30aba420aadc7c4`. This is same-machine unpacked-package evidence, not installer, distinct clean-machine, hosted CI, production, formal or user-visual acceptance.
  - c4_packaged_golden_journeys_first_pass_candidate: PASS_CANDIDATE at PACKAGED_SAME_MACHINE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL scope — exact root `artifacts/v3-v1-1-release-lfxQNr` contains successful A create/reopen and B create/reopen evidence. A preserved exact canonical identities across restart and exposed a VALID Result with orders/fills; B matched an independently computed 20-symbol daily IC 0.9962117698528705, Rank IC 1, five quantiles and spread 0.010367444606823595 before and after restart. All four phases proved packaged runtime, graceful shutdown, no known-ID injection and no orphan backend; copied package identity before/after is 1,049,843,446 bytes / 13,903 files / SHA-256 `af367c50ce295468aded254f98bfcf5e36d36012059e1f6d04f3616593dd03a7`. Evidence label `renderer_store_instance` is inaccurate for V1.1 and requires one bounded correction/rerun before final C4 evidence use.
  - c4_packaged_renderer_process_evidence_red_green: EXPECTED FAIL then PASS — the source/effect audit found V1.1 create/reopen evidence mislabeled as `ISOLATED_UNAVAILABLE_PROCESS`. A closed contract first failed on the absent classifier; the new owner maps create to `FIRST_PROCESS`, reopen to `NEW_PROCESS`, and provider-unavailable to the isolated label. Owner/ProductBridge tests pass 29/29 after build. Packaged rerun is NOT_RUN after this evidence-only correction.
  - c4_packaged_result_analytics_identity: EXPECTED FAIL then PASS at source/bridge scope — the rebuilt real product crossed Data, Context, Factor, Strategy, Backtest execution and Task readback, then `latestProductResultDetails` rejected the Result Analytics identity against Project Home. Read-only catalog plus exact Artifact bytes proved no canonical drift: the Backtest summary and Analytics Artifact correctly named two different owner engines. The fixture had hidden the bug by assigning the Analytics engine to Home. With realistic distinct values, the bridge test reproduced the packaged exception, then passed after ProductBridge validated `v3.result_analytics_engine/1.1.0` independently while retaining the Home Backtest engine. Full bridge/renderer is 49/49, typecheck and build PASS. Packaged rerun remains NOT_RUN after this fix.
  - c4_backtest_task_legacy_success_shape: EXPECTED FAIL then PASS — the fifth real packaged Journey A completed Backtest and persisted a VALID Result plus 12 relational outputs, then TaskService readback rejected the legitimate legacy/event-specific success payload because it has no nested `outputs`. The real Product Backtest regression reproduced the exact exception. `_task_read_model` now accepts an absent legacy `outputs` member and resolves durable Run/task-output truth, but rejects a present non-object or non-empty-string mapping and any conflicting role value. The append-only catalog rejected an attempted in-place corruption; the negative uses an additional malformed terminal event instead. Focused plus adjacent Local Data/Backtest/Product Research/Runtime Integrity is 40/40 PASS. Packaged rerun remains NOT_RUN after this fix.
  - c4_backtest_event_page_contract_red_green: EXPECTED FAIL then PASS — the fourth real packaged Journey A reached Backtest and failed before transport with `INVALID_ARGUMENT · limit must be an integer in [1, 500]`. Source traced two renderer calls using 1000 despite the closed bridge/IPC ceiling. Focused tests asserted the actual limit and failed both initial Backtest and retry paths; the shared caller-side limit is now 500 and its initial event cursor uses the same bound. Renderer focus PASSes 21/21; no backend limit was relaxed. Packaged rerun remains NOT_RUN after this fix.
  - c4_factor_home_transport_projection_red_green: EXPECTED FAIL then PASS — after both earlier corrections, the real packaged Factor Task reached `SUCCEEDED`, but backend encoding failed closed with `frame exceeds negotiated maximum`. Exact catalog evidence shows the full Product Factor read-model Artifact is 2,721,591 bytes and its Analysis Artifact is 1,966,537 bytes versus the negotiated 1,048,576-byte frame. The correction does not raise the ceiling or discard canonical bytes: full Artifacts remain content-addressed and project-reachable, while Project Home schema v1.1 returns the latest at most 256 rows in canonical ascending order plus exact total counts and fixed projection tokens for both visual and daily-analysis views. A product-sized 3,200-row regression failed against v1.0/full-inline behavior then PASSed below the frame ceiling. Factor entry/persistence 5/5, contract seed/hardening 15/15, ProductBridge/renderer 49/49, typecheck and 35-module build PASS. Packaged rerun remains NOT_RUN after this fix.
  - c4_terminal_task_process_exit_race_red_green: EXPECTED FAIL then PASS — the second real packaged Journey A crossed the Task read-model correction, completed local import, then backend shutdown during atomic context rebinding emitted `RUNTIME_INTERNAL_ERROR`; the bridge surfaced `BINDING_ACTIVATION_ROLLBACK_FAILED`. Catalog inspection proved the Local Data Task/Run were already terminal and its new context outputs durable. Source tracing found `_product_worker_main` persists Task success before sending its terminal protocol message and exiting, while `prepare_shutdown` called `cancel_research_task`, which correctly refuses terminal tasks, and then immediately rejected the still-live child. The new deterministic regression reproduced that exact ordering and failed before the correction. `ProductResearchWorkerManager.confirm_terminal_exit` now waits/escalates OS-process exit without signalling canonical Task cancellation; non-terminal shutdown keeps the existing cancellation path. The regression and the existing running-worker shutdown truth test PASS 2/2. Packaged rerun remains NOT_RUN after this fix.
  - c4_task_terminal_output_red_green: EXPECTED FAIL then PASS — the first real packaged Journey A reached a successful `ProductEntryService.v1.importLocalDataset` Task but failed because `TaskService.v1.getTask` returned no `project_context_revision_id`/`snapshot_id`. SQLite and source tracing proved `_finish_success` durably writes those semantic outputs into the `TASK_SUCCEEDED` event while `_task_read_model` read only Run Artifact references and ignored both terminal-event outputs and `task_output`. A real ProductEntry → isolated worker → TaskService regression reproduced `KeyError: project_context_revision_id`; the bounded read model now merges exact Run references, relational `task_output`, and scalar durable success-event outputs with conflict rejection. The same test PASSes 1/1 and the adjacent Local Data, Factor ProductEntry and Task operation set PASSes 10/10 from D-drive temporary roots. Packaged rerun remains NOT_RUN after this fix.
  - c4_packaged_journey_a_first_real_run: FAIL at PACKAGED_SAME_MACHINE scope — the unpacked Electron product launched with its staged bundled Python/runtime and imported the user-selected local CSV through the real bridge, but stopped after the Local Data Task reached `SUCCEEDED` because the Task read model omitted the canonical context outputs required for atomic renderer rebinding. This is a production contract/read-model defect, not a fixture or visual assertion. Journey A, Journey B and restart remain NOT_RUN until the rebuilt candidate crosses this boundary.
  - c4_packaged_journey_phase_contract_red: EXPECTED FAIL — after a successful 35-module production build, the focused C4 contract reaches the built `productClosureSmoke` owner and fails because the four V1.1 Journey A/B create/reopen phases and their `LOCAL_USER_SUPPLIED` boundary functions do not exist. The initial invocation had two infrastructure-only failures before the assertion (sandbox denied a D-drive temp root outside its writable binding; one Windows dist unlink returned transient EPERM). The corrected D-drive worktree invocation produces the intended missing-owner red result; no assertion was weakened.
  - c3_final_current_validation_and_effect_audit: PASS at LOCAL_WORKTREE / PRODUCT_CONNECTED / PRE_ALPHA / RESEARCH_ONLY / NOT_FORMAL scope — the exact current 71-file expanded delta passes `validate:public` with exit 0. The immediately prior required Product smoke run passes Data, Factor, Backtest, Result, Product Runtime (53 checks) and Electron runtime/restart. Post-green source tracing found the generic `*Registry` owner guard falsely treating the plan-required domain `ExecutionPolicyRegistry` as a persistence registry; the guard now preserves exactly one `*RepositoryRegistry`, with its focused suite 8/8. Source tracing also changed failed native export from hidden tmp retention to exact one-use tmp cleanup and projected already-verified fee components into the required costs table. Build 35 modules, ProductBridge/export 31/31, typecheck and lint pass after those two corrections, and a second full `validate:public` confirms the current diff. No packaged, clean-machine, remote, PR or formal capability claim is made.
  - c3_plan_14_7_14_9_gap_closure_focus: PASS at LOCAL_WORKTREE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL scope — ProductEntry registry/fixtures are 74 operations and 10 methods with contract seed/hardening 15/15 PASS. V1.1 Backtest/Analytics is 18/18 PASS, including side-effect-free exact-owner preflight, durable crash recovery with 12 relational outputs, deterministic summary JSON/orders CSV/fills CSV source binding and restart reconstruction. ProductBridge is 28/28; desktop/renderer focus is 49/49 and the updated cold-store suite 21/21. A real Electron closed-shape omission for the three export refs was corrected rather than weakening the adapter. Typecheck, lint and the 35-module production build pass; one transient first-build EPERM was followed by an unchanged PASS rerun. Full `validate:public` and product smokes remain NOT_RUN after this delta.
  - c3_artifact_batch_input_fence: PASS — ProductArtifactBatch now validates 4/5-field payload tuples, bytes, role/schema/provenance and MIME before any staging side effect; the direct malformed-tuple negative passes 1/1.
  - c3_result_service_14_5_red_green: EXPECTED FAIL then PASS — the production facade initially lacked reconcileLedger/finalizeResult/compareResults and getResult was summary-only. The bounded service now re-verifies current ProjectContext, the sole VALID Product Result, the sole FINALIZED publication intent, actual ledger/analytics/lineage and all upstream owner bytes in both main preflight and isolated child execution. Reconcile/finalize reuse existing canonical IDs and emit durable relational verification outputs without minting Result or PublicationIntent; same-key replay returns the same Task/Run, different payload conflicts, wrong owner fails before Task, all seven getResult sections are exact, and compare fails with RESULT_COMPARISON_NOT_AVAILABLE. Focused ResultService/capability/legacy restart set is 4/4 PASS from D-drive temporary roots. Full C3/repository validation remains NOT_RUN after this delta.
  - c3_result_read_model_and_local_source_audit: FAIL then focused PASS — source review found Result table sections echoing requested pagination while returning a whole Artifact stream; they now truthfully report FULL_ARTIFACT_STREAM, row_pagination_applied=false and reject non-empty cursors. The first full validate:public then found the Windows same-size local-source replacement race because metadata-only dev/ino/size/mtime/ctime fencing can remain unchanged. Selection and pre-transfer now stream-hash the same open handle, transfer completion compares the streamed digest, and the focused Electron suite is 3/3 PASS. The complete post-fix rerun process exited after Codex compaction lost its exec output/exit handle, so that run is recorded UNKNOWN and is not promoted.
  - acc_c3_06_product_corporate_action_red: EXPECTED FAIL — focused D-drive-temp acceptance fails at import because `ProductCorporateActionService` is absent. Its unchanged assertions require a project-reachable content-addressed owner payload, a non-empty CORPORATE_ACTIONS exact reference, one actual action in RunSpec, positive cash dividend effect and positive integral split position effect.
  - acc_c3_06_product_corporate_action_green: PASS — two positive Product cases resolve actual project-owned cax/artifact bytes to exact Snapshot rows, preserve PRE_ALPHA/NOT_FORMAL truth, feed the sole Backtest engine and produce positive dividend/split ledger effects. Four negative cases reject fractional, RIGHTS_ISSUE, DELISTING and OTHER before Task creation with CORPORATE_ACTION_NOT_AVAILABLE. Product Backtest/Data/Factor focus is 22/22 PASS; corrected standalone sole-engine suite is 40/40 PASS. The initial aggregate engine invocation had only an incomplete test PYTHONPATH and never entered engine code; it was not treated as a product failure. No-action imports retain the existing v1.1 manifest shape/identity path; only action-bearing imports use additive v1.2 refs.
  - c3_product_backtest_retry_red: EXPECTED FAIL — a durably accepted Product Backtest was failed with TRANSIENT_IO and its accepted read model advertised NEW_ATTEMPT_SAME_RUN_FROM_START, but TaskService.v1.retryTask returned FAILED because the execution context kind PRODUCT_RESEARCH_BACKTEST was unsupported.
  - c3_product_backtest_retry_backend_green: PASS — focused acceptance 1/1. Retry verifies the exact failed Attempt, persisted category, immutable Product execution-context Artifact and Run semantic identity; it preserves Task/Run identity, creates Attempt ordinal 2 and dispatches the real isolated Backtest worker to a VALID terminal Result. This is LOCAL_WORKTREE evidence only; Electron/renderer action remains NOT_RUN.
  - c3_product_backtest_retry_finality_green: PASS — a focused second-failure acceptance proves execute_accepted passes run_transition=false when the immutable Run is already TERMINAL; combined retry focus is 2/2 and adjacent Backtest/Analytics/ProductRuntime/RuntimeIntegrity is 59/59 PASS from D-drive TEMP/TMP/PYTHONPYCACHEPREFIX.
  - c3_product_backtest_retry_product_green: PASS — Electron main re-reads exact persisted Task state and sends only owner-derived failed Attempt/state-version coordinates; INVALID_ARGUMENT never reaches retry transport. Closed IPC/preload, renderer scope fencing, retry-admitted visibility and canonical VALID readback pass. Focused 53/53, complete Node unit 149/149, typecheck/lint/build and diff check PASS.
  - acc_c3_01_product_backtest_runtime_session: PASS — direct ProductEntry submission returns durable QUEUED before owner execution; while the configured isolated child is live, a correlated RuntimeSession health/getTask/getEvents batch completes under 0.5 seconds and returns the exact persisted Task/events. Focused acceptance 1/1 from D-drive temp; this is LOCAL_WORKTREE evidence, not packaged proof.
  - c3_legacy_backtest_capability_red: EXPECTED FAIL — the new production-ports acceptance reached the current handshake and observed `BacktestService.truth_state=FORMAL`; it failed before request assertions. This confirms the plan 5.5 mismatch in the real production composition. A requested D-drive temp directory outside the writable worktree was denied, so subsequent runs will use a D-drive temp root inside this worktree; no C-drive evidence is claimed.
  - c3_legacy_backtest_capability_green: PASS — normal production capability is UNAVAILABLE with exact reason FORMAL_EXECUTION_CONTRACT_NOT_CLOSED; all four frozen operations remain wire-valid but execute only a denial facade and create no Task/Event side effect. ProductBridge blocks the legacy call before transport while the additive ProductEntry research Backtest path remains unchanged. Backend ProductRuntime/Integrity 46/46; affected Desktop/ProductBridge/cross-language 55/55; typecheck, lint and 35-module build PASS; normal Product Runtime smoke 53/53 with restart/no legacy events. The legacy implementation is exercised only through an explicit internal test composition and is not product capability.
  - c3_policy_progress_preview_source_delta: PASS at LOCAL_WORKTREE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL scope — backend-owned closed policy coverage is shared by Home and preflight; Product execution emits durable bounded progress events; Strategy preview resolves exact owner payloads, compiles through the existing canonical Strategy owner and advertises side_effects=NONE; renderer requires an exact matching preview before Publish.
  - c3_policy_progress_preview_typecheck: PASS — `npm.cmd run typecheck` after the coverage/progress/preview TypeScript and bridge changes.
  - c3_policy_progress_preview_python_focus: UNKNOWN — the first run failed at registry import because its frozen operation count was 72; the source/contract expectation was corrected to 73. Automatic compaction truncated the post-correction aggregate output, so no PASS/FAIL is inferred. The exact next action is the concise rerun.
  - c3_policy_progress_preview_python_rerun: PASS — concise Strategy authoring/ProductEntry/Backtest focus emitted 15 passing cases with exit 0; unchanged added preview/progress/coverage focus subsequently passed after one real rule-profile prefix contract correction.
  - c3_policy_progress_preview_contracts: FAIL then PASS — Home response validation exposed `rules_sha256_` while the actual Rule owner emits `atrp_sha256_`; Python contract and Electron adapter now use the exact owner prefix. ProductEntry fixture regeneration initially omitted method error envelopes, then the bounded generator was corrected to preserve the frozen envelope and add truthful preview notes. Contract seed/hardening is 15/15 PASS.
  - c3_policy_progress_preview_product: PASS — ProductBridge 27/27 rejects missing/drifted policy coverage and validates preview against current Home/intent; renderer store 20/20 refuses Publish without an exact matching preview and recovers durable RECONCILING progress; desktop/UI focus 27/27 plus Backtest Workspace source gate 4/4 pass. Typecheck, 35-module production build and lint pass.
  - c3_expanded_validation_after_preview: FAIL then PASS — full Node unit found only the unrelated deterministic development research-series assertion accidentally changed from its frozen 72 to the global contract count 73; source and HEAD prove the series remains 72, so only that stale test edit was reverted. Complete Node unit is 148/148 PASS. Product Backtest/Result smokes prove VALID exact artifacts and restart readback without recomputation; Product Runtime is 71/71; Electron runtime process smoke passes. Full validate:public exits 0 across authority/typecheck/lint, 74 backend files/41 suites, build, bundle/manifest, Electron 117/117, C1 fault 29+78, frontend, secret, repository and SBOM gates.
  - c3_desktop_strategy_backtest_green: PASS — strict Product Home projection, backend-owned authoring profile, fresh-Home Strategy/Backtest ProductEntry submissions, typed IPC/preload, forbidden caller financial payload rejection and a second bridge instance reading VALID Result/Analytics/Lineage without submission are green. ProductBridge is 27/27; contract seed/hardening is 15/15; backend ACC-C3-13 focus passes.
  - c3_renderer_first_connection: PASS at LOCAL_WORKTREE / PRODUCT_CONNECTED scope — PRODUCT navigation enables Backtest from canonical Factor readiness and Results only for a latest VALID Backtest. Chinese-first low-chrome workspaces author bounded Strategy/Backtest intent and project verified Result metrics/charts/tables/lineage with native exact-Artifact export. Typecheck, lint and production build pass; focused store restart/scope acceptance is 20/20 and the prior full unit set is superseded by 146/146 PASS inside the currently running validate:public command.
  - acc_c3_13_product_entry_home_red: EXPECTED FAIL — the unchanged acceptance fails at the missing ProductEntryService.v1.submitResearchBacktest handler before execution. It already requires a restarted ProductRuntime to rebuild latest Strategy and VALID Backtest summaries from canonical owner read models, with no embedded orders/fills/analytics payloads.
  - acc_c3_11_analytics_golden: PASS — 2/2 additive V1.1 analytics tests independently prove Calmar, gross/net exposure, position concentration, explicit no-position/zero-drawdown states and BENCHMARK_NOT_AVAILABLE. The unchanged V0 analytics suite is 27/27 PASS, including corrected research-0.3 turnover/cost attribution against execution_price rather than raw_price.
  - acc_c3_12_result_lineage_publication: PASS — the Product publication saga emits a content-addressed PRODUCT_RESULT_LINEAGE Artifact binding project/context, RawCapture/Snapshot, exact entry/exit Factor refs, Signal/TargetWeight/RiskPolicy decision chains, Orders/Fills, Result and Analytics. It is a required relational Task output and restart read-model field. Combined analytics/publication/crash focus is 5/5 PASS. Restart-time downstream owner re-resolution and a tampered-lineage negative remain the exact active step.
  - acc_c3_12_result_lineage_restart: PASS — get_backtest requires each Result output to be both project-reachable and actively referenced from the exact Run/Result owner, verifies canonical Result/ledger/Analytics/Lineage payloads, reopens exact Snapshot partitions, Universe, RawCapture, Factor, Strategy, Portfolio and Risk bytes, and compares the full decision/order/fill projection. A semantically wrong but freshly content-addressed and project-reachable lineage is rejected after restart. Combined C3-11/12 suite is 5/5 PASS; git diff --check PASS.
  - c3_publication_crash_recovery_red: EXPECTED FAIL — both AFTER_CATALOG_COMMITTED and AFTER_RECONCILIATION_PUBLISHED simulated process crashes leave durable CATALOG_COMMITTED/PENDING_RECONCILIATION state and no false Task success; restart currently does not reconcile either intent, so the unchanged acceptance fails twice on expected FINALIZED.
  - c3_publication_crash_recovery_green: PASS — six-boundary matrix covers BEFORE_INTENT_COMMIT, AFTER_INTENT_STAGED, AFTER_INITIAL_ARTIFACTS_PUBLISHED, AFTER_CATALOG_COMMITTED, AFTER_RECONCILIATION_PUBLISHED and DURING_FINALIZE_TRANSACTION. Every durable intent reaches one FINALIZED or FAILED terminal; valid cataloged bytes recover to Result VALID/Task SUCCEEDED atomically and idempotently, early incomplete intents fail closed, and pre-intent active worker loss is fenced by generic restart reconciliation. Complete focused C3/Backtest/Product Runtime regression is 106/106 PASS.
  - c3_reconciliation_corruption_red: EXPECTED FAIL — all five semantic corruptions are detected with no VALID read model and terminal INVALID/FAILED states, but each lacks the required durable failure reconciliation Artifact; the focused acceptance fails five times only on null reconciliation_artifact_id.
  - c3_reconciliation_corruption_green: PASS — five semantic corruptions retain self-consistent content identities so reconciliation logic, not only Artifact hashing, rejects them. Each terminal INVALID Result carries a verified FAIL receipt with RESIDUAL_VALIDATION_FAILED; Task/intent are FAILED and no VALID read model is reachable. Focused adjacent regression is 107/107 PASS.
  - c3_stream_source_audit: current ArtifactFacade ticket registry has TTL/capacity but open/getDescriptor trust global PUBLISHED identity without project reachability. Ticket contains project/range internally but has no consume path or runtime generation; Electron ProductBridge/preload expose only ticket metadata. First cross-project open test is EXPECTED FAIL.
  - c3_stream_reachability_green: PASS — the unchanged cross-project acceptance now rejects Project B opening Project A's Result Artifact through one reusable ProductRuntime active-reference verifier covering Project/Task/Run/TaskAttempt/Result owners.
  - c3_stream_lifecycle_red: EXPECTED FAIL — two new tests fail at the missing ArtifactFacade runtime_generation/consume API before any stream assertion; their unchanged assertions fix expiry, replay, wrong generation, <=256 KiB chunk hashes and exact large-artifact reassembly.
  - c3_stream_backend_transport_green: PASS — ArtifactFacade revalidates exact ProjectContext and active project reachability, prunes expiry, binds runtime generation, consumes once only after reusable checks, reads through the verified Artifact handle, and emits <=256 KiB chunk hashes plus exact terminal size/full hash. RuntimeSession exposes only the correlated closed `v3.artifact-stream/1.0.0` control path. Focused backend acceptance is 4/4 PASS.
  - c3_stream_electron_supervisor_green: PASS — the current-generation correlated receiver rejects stale/mismatched/corrupt/noncanonical frames, reassembles exact bytes, rechecks chunk/full SHA-256 and size, and includes stream requests in bounded capacity/rejection lifecycle. Supervisor suite is 23/23 PASS; typecheck/build PASS with dirty local BuildManifest only.
  - c3_stream_product_bridge_red: EXPECTED FAIL — unchanged ProductBridge test reaches the descriptor/open/consume stub path and fails only because `readArtifactBytes` is absent. No test assertion was weakened; interrupted patch application remains UNKNOWN until source inspection.
  - c3_stream_product_bridge_green: PASS — ProductBridge resolves the descriptor, opens an exact ticket, asks the Supervisor to consume against descriptor ID/hash/size, revalidates the returned identity and exposes a copied Uint8Array through one closed IPC/preload method. ProductBridge plus framing/boundary focus is 30/30 PASS.
  - acc_c3_09_stream_security: PASS at LOCAL_WORKTREE / PRODUCT_CONNECTED transport scope — cross-project open, expiry, replay, wrong generation and corrupt chunk hash reject; each chunk is <=256 KiB and independently hashed; terminal size/full hash and descriptor identity are rechecked. A real 614,414-byte project-reachable Artifact crosses Python bootstrap/RuntimeSession/Electron Supervisor/ProductBridge and reassembles byte-for-byte. This does not prove packaged or clean-machine operation and does not promote semantic truth of Artifact contents.
  - acc_c3_10_export_truth: PASS at LOCAL_WORKTREE / PRODUCT_CONNECTED transport scope — native chooser cancellation returns NOT_RUN without a Task; accepted export streams exact verified bytes into an exclusive same-directory tmp, fsyncs, rechecks hash/size, atomically renames and only then completes the durable backend Task. The earlier focused state retained an identifiable tmp after mid-stream failure; the final source/effect audit supersedes that behavior by deleting the exact one-use tmp before recording a FAILED Task without manifest. The public manifest records filename/bytes/hash/completed_at but not the opaque destination capability. Evidence: typecheck PASS; build PASS after one recorded transient Windows EPERM retry; Electron Supervisor/ProductBridge/broker 55/55; backend ProductRuntime/RuntimeSession 55/55; real Python↔Electron cross-language stream/export 2/2; final broker/ProductBridge focus 31/31 and exact-current `validate:public` exit 0. This does not prove packaged or clean-machine export.
  - c3_strategy_authoring_red_green: EXPECTED FAIL then PASS — the recovered 1,034-line patch first failed on the real SQLite export boundary, then the role allow-list and exact Strategy registry contract; the unchanged acceptance now publishes verified C2 boolean Factor/state inputs through existing Strategy/Portfolio/Risk owners, yields sig/sel/pint/twv/rar/rawv identities, and restarts to the same PRE_ALPHA/NOT_FORMAL read model. Direct Strategy runtime values are decoded from the just-published verified Artifact bytes; no caller/renderer weights or Factor preview are used.
  - c3_strategy_authoring_negative: PASS — 5/5 focused tests. Cross-project context, simultaneous entry/exit, tampered actual Factor bytes and released Strategy state links fail closed; normal restart reopens and verifies all project-linked Strategy payloads plus canonical TargetWeight/Risk publication bytes and context.
  - c3_strategy_product_entry_green: PASS — 2/2 focused entry tests and 7/7 complete Strategy discovery before the latest compaction. The closed 1.1 request contains refs and bounded intent only; durable Task acceptance precedes Strategy publication, idempotent replay returns the same Task, isolated STRATEGY_AUTHORING execution completes and restart readback matches.
  - c3_strategy_contract_green: PASS — 15/15 contract seed/hardening tests before the latest compaction. ProductEntryService now has seven operations and the global contract registry has 71 operations; executable fixtures, conformance manifest and seed digest were synchronized. This must be re-run in the adjacent regression pass below.
  - c3_adjacent_regression_after_entry: NOT_RUN after recovery — the previous combined invocation output was truncated by automatic compaction, so no aggregate result is inferred. Separate bounded reruns are the exact next action.
  - c3_strategy_exact_factor_red_green: EXPECTED FAIL then PASS — a later same-context Factor study previously shadowed the exact entry/exit refs because Strategy queried only the newest study; exact FactorVersion selection now resolves and fully verifies the referenced owner read model/materialization.
  - c3_strategy_factor_recovery_red_green: EXPECTED FAIL then PASS — restart previously accepted a Strategy after its referenced Factor partition bytes were tampered; get_strategy now re-resolves both exact Factor refs and reads/verifies their actual manifest/partition bytes before returning.
  - c3_strategy_catalog_red_green: EXPECTED FAIL then PASS — StrategyDefinition artifacts previously had no strategy_version Catalog owner even though backtest_run_spec has a required FK. The bounded publication now stores Strategy IR + validation Artifacts, an idempotent PUBLISHED strategy_version row and exact readback checks; RETIRED state fails closed.
  - c3_strategy_after_source_review: PASS — Strategy 10/10; full V1.1 Factor 9/9; Product Runtime 55/55; Runtime Integrity 5/5; contract seed/hardening 15/15; source compile via in-memory compile 3/3; lint PASS. A direct compileall attempt was INFRA FAIL only because the worktree runtime __pycache__ is not writable; unchanged in-memory compile passed without creating C-drive output.
  - c3_strategy_section_14_1: PASS at LOCAL_WORKTREE / PRE_ALPHA / NOT_FORMAL scope. This proves bounded backend Strategy authoring and durable ProductEntry, not Backtest/Result UI, packaged product, clean machine, exact-SHA review or production availability.
  - c3_backtest_future_holding_red_green: EXPECTED FAIL then PASS — BacktestRunSpec now rejects acquired_on later than its first admitted session before engine execution; the full track_j A-share core suite is 32/32 PASS.
  - c3_research_execution_red: EXPECTED FAIL — after the model types were recovered as fully applied, the complete core suite ran 35 tests: legacy/future-holding tests 01..32 PASS; tests 33..35 error only at the missing engine keyword `research_execution`. The fixed acceptance requires exact policy/market binding, adverse slippage, participation partial fills and fail-closed missing/mismatched inputs; no assertion was weakened.
  - c3_research_execution_green: PASS — 39/39 complete A-share core tests. Research 0.3 rejects missing/mismatched policy and market refs, incomplete liquidity coverage and zero executable volume; buy/sell slippage is adverse and tick-rounded, volume creates real partial fills, policy changes alter RunSpec/Fill/Result identities, a 100-billion-share liquidity case stays below the 2-second bounded-affordability gate with non-negative cash, and legacy 0.2 wire contains none of the additive fields.
  - c3_backtest_adjacent_regression: PASS — FormalBacktest exact-payload suite 21/21 plus Product Runtime 36/36 (57/57 aggregate). The frozen Formal/legacy Product adapter remains on 0.2, verified actual payload rejection remains intact, and pending Result limitations are not promoted.
  - c3_engine_version_regression_red_green: FAIL then PASS — full validate:public exposed one legacy comparison fixture asking the production engine to execute unknown version 9.9.9. The production fail-closed check was retained and now has a direct core assertion; comparison coverage uses a test-only subclass that explicitly declares the alternate implemented version before creating its result. Focused Backtest plus Portfolio/Risk Agent is 136/136 PASS. No production version allow-list was weakened.
  - c3_validate_public_after_engine_fix: PASS — authority/typecheck/lint; Node unit 146/146; backend 41 suites, 74 files, 1062 cases; 35-module production build; bundle truth and DIRTY BuildManifest verification; Electron runtime 117/117; C1 fault matrix 29 Python plus 78 Electron; frontend smoke; secret scan over 739 current files/relevant history; repository audit over 739 files/12,285,490 bytes; SBOM 518 entries.
  - c3_required_product_smokes: PASS at LOCAL_WORKTREE scope — Data PASS with restart; Factor PASS with single-symbol INSUFFICIENT_SAMPLE and restart; Backtest PASS with VALID Result/exact Result+Analytics+Lineage artifacts and no restart recompute; Result PASS with the same truth/finality checks; product-runtime PASS 71/71; Electron runtime PASS across capture/restart, authenticated READY, exactly-once replay, durable cursor, single-instance fencing and graceful shutdown. The locked Electron 39.8.10 archive checksum matched package authority before installation, and the smoke runner now uses typed ProductRuntime status plus D-drive product storage rather than the obsolete Agent DOM route. This remains PRODUCT_CONNECTED / PRE_ALPHA / RESEARCH_ONLY / NOT_FORMAL and is not packaged or clean-machine proof.
  - c3_strict_and_corporate_source_audit_red_green: EXPECTED FAIL then PASS — the first 2-test run exposed zero-slippage rejection plus a test-only wrong TaskEvent order column; after correcting the test query and bounded owners, both tests pass. STRICT_FAIL_CLOSED now requires explicit Snapshot suspension/ST/tradability/no-limit/limit values, binds those verified bytes into DailyMarketState and persists a strict AssumptionReceipt/read model through restart. Missing state fails closed; any canonical corporate_action_ref without an admitted action owner fails before engine/publication, leaves Task FAILED and creates no VALID Result/read model. No formal truth is claimed.
  - c3_backtest_preflight_policy_coverage: PASS — focused V1.1 Backtest suite 9/9 from D-drive TEMP/TMP/PYTHONPYCACHEPREFIX. Lightweight preflight resolves the exact latest Strategy and published Snapshot, verifies date coverage and the backend-owned cost/rule/timing/assumption identities, and rejects outside the actual 2026-07-06+ intersection with EXECUTION_POLICY_COVERAGE_UNAVAILABLE before Task creation. Snapshot coverage diagnostics retain precedence when both ranges are invalid.
  - c3_product_research_backtest_red: EXPECTED FAIL — the unchanged ACC-C3-01 acceptance fails at import only because v3_backend.runtime.product_backtest is absent. Its assertions require durable QUEUED-before-resolution, actual Snapshot/Strategy/TargetWeight/Risk resolution, 0.3-research execution, exact next-open timing, FINALIZED publication intent, VALID Result before Task success, relational task outputs and restart-identical PRE_ALPHA/NOT_FORMAL readback.
  - c3_product_research_backtest_green: PASS — unchanged ACC-C3-01 1/1. Durable acceptance returns QUEUED before resolution; the isolated child resolves actual Data/Strategy/Risk owner bytes, runs the sole 0.3-research engine and publishes a restart-readable NOT_FORMAL/PRE_ALPHA result. The source-level 09:30/cutoff defect was corrected to 09:14:59 so t-close executes at the t+1 09:25 open. Result VALID, PublicationIntent FINALIZED, seven relational task outputs and Task SUCCEEDED are committed together only after exact-byte/ledger reconciliation.
  - c3_product_entry_home_green: PASS — focused ACC-C3-13 backend restart acceptance plus 15/15 ProductEntry executable contract seed/hardening. ProductEntry has 8 methods / registry 72 operations; restarted Home exposes only owner-revalidated Strategy and VALID Backtest summaries, never orders/fills/analytics.
  - c3_strategy_authoring_profile_red_green: EXPECTED FAIL then PASS — Home initially lacked the backend-owned cost/execution/risk/assumption refs required by publishResearchStrategy. The unchanged acceptance now receives one closed PRE_ALPHA/NOT_FORMAL authoring profile from ProductStrategyService; Desktop has no reason to hard-code or invent a second profile authority.
  - c3_desktop_strategy_backtest_red: EXPECTED FAIL — after the profile addition, the strict Desktop adapter rejects the expanded closed Home shape. The fixed acceptance additionally requires main-owned fresh-Home resolution, typed Strategy/Backtest methods, canonical refs supplied only by Home, closed idempotent ProductEntry payloads, and zero caller bars/factor values/weights/portfolio/risk payloads.
  - c3_backtest_new_adjacent_regression: PASS — 105/105 focused V1.1 Backtest, Strategy, A-share engine and Product Runtime tests. A prior package-directory discovery invocation ran 0 tests, and a second discovery form imported the relative Strategy module incorrectly; neither reached product assertions. The corrected explicit-module command passed. git diff --check and npm lint also PASS.
  - last_backend_command: D-drive TEMP/TMP/PYTHONPYCACHEPREFIX; npm.cmd run test:backend
  - backend_result: PASS — aggregate exit 0; 39/39 suites, 70/70 discovered files, 1031/1031 tests, compile gate passed
  - final_required_commands: PASS for C2 — validate:authority; typecheck; lint; test:unit 143/143; test:backend 1031/1031; build 33 modules; test:runtime 104/104; test:c1-faults 27 Python + 70 Electron; bundle/manifest/frontend/Data/Factor smokes; Data 16/16; secret/repo/SBOM; diff/P0
  - build_manifest: bmanifest_sha256_145ce4ecc68932f738b7936bca0be41cc0586cec6396d154d13b444dc9ed45b7; DIRTY local build only
  - evidence_path: exact external D-drive temporary roots under D:\V3OpenSource-worktrees\.tmp\v1-1-c2-final; no cache/temp file entered commit 07cefa53e16ebed277a2bd1423f851379aa4732b
  - c2_owner_boundary_regression: PASS — ScorePayloadOwnerBoundaryTests proves direct exact-Universe encoding equals the legacy wrapper bytes and contains neither owner_id nor sgv_placeholder
  - c1_deadline_determinism_regression: PASS — the test observes a real DISPATCHED child before deadline cancellation and retains the exact TERMINATE_SENT/TERMINATED trace assertion
  - c2_local_data_red: EXPECTED FAIL — LocalDataImportError import was absent before the owner implementation
  - c2_local_data_narrow: PASS — 5 tests cover closed UTF-8 CSV, resource bounds, duplicate/OHLC rejection, nested Parquet rejection, SHARES/HANDS equivalence, MJ unit identity and CSV/Parquet normalized hash equivalence
  - c2_dependency_truth: PARTIAL PASS — requirements exact pin, checked-in CPython 3.14 win_amd64 wheel URL/SHA, packaged critical import, regenerated license matrix/SPDX and sbom:check PASS; actual packaged build/import/size delta remains NOT_RUN
  - c2_full_backend_inventory: FAIL then targeted correction PASS — the first 63-file/38-suite run reached the new suite but unittest `-t apps/backend/tests` rejected it because the new directory lacked __init__.py; added the package marker and reran the exact discovery form successfully at 5/5. A new complete backend run after this correction remains NOT_RUN.
  - c2_product_data_persistence_red: EXPECTED FAIL — `py -3.14 -B -m unittest apps.backend.tests.v1_1_local_data.test_product_data_persistence -v` fails at import with `ModuleNotFoundError: No module named 'v3_backend.runtime.product_data'`; no assertion was weakened and no implementation exists yet.
  - c2_product_data_persistence_green: PASS — 3 application tests prove exact raw CSV/Parquet bytes/media, project-scoped Artifact roles, PRE_ALPHA Snapshot/RawCapture/USER_DEFINED_STATIC Universe/ProjectContext persistence, restart readback, same-project format equivalence, immutable published Snapshot source membership, cross-project rejection and invalid-input zero publication.
  - c2_product_data_entry_red: EXPECTED FAIL — 2/2 tests fail because ProductEntryService.v1.importLocalDataset is absent from the registry and therefore unknown to RequestRouter. The acceptance fixes the additive 1.1 ref-only schema, legacy 1.0 compatibility, durable Task-before-Snapshot ordering, isolated execution and PRE_ALPHA/NOT_FORMAL readback; no assertion was weakened.
  - c2_product_data_entry_green: PASS — 4/4 Product Entry tests cover additive 1.1 closed schema and fixture, exact legacy 1.0 operation/version preservation, sub-two-second durable QUEUED acceptance before Snapshot work, isolated completion, idempotent replay, cross-project pre-Task rejection and actual-byte tamper failure with zero Snapshot.
  - c2_contract_regression: PASS — 15/15 contract seed/hardening tests; 18 services/68 operations, ProductEntryService API 1.1.0 with the original three 1.0 operations as the exact ordered prefix, fixture manifest and seed digest match.
  - c2_shared_worker_regression: PASS — 50/50 Product Runtime + legacy Product Research tests and 5/5 runtime-integrity tests after the worker manager became a closed work-kind dispatcher; 1.0 research behavior remains green.
  - c2_artifact_stream_regression: PASS — 36/36 Artifact owner tests plus the local-data tamper test; published source verification hashes the same open handle and does not load the source into memory.
  - c2_local_data_suite: PASS — 12/12 importer + Product Entry + application/persistence tests after compaction recovery, including actual-byte tamper failure.
  - c2_native_transfer_red: EXPECTED FAIL — focused Electron acceptance cannot import dist/apps/desktop/src/main/productRuntime/localDataImport.js because the owner is not implemented. The immediately preceding build attempt also hit EPERM while unlinking a prior dist contract copy; this is recorded separately and must be rechecked after implementation rather than treated as the acceptance cause.
  - c2_native_transfer_green: PASS — 45/45 focused Electron tests prove chooser cancellation, renderer path non-disclosure, non-regular/reparse/replacement-race rejection, one-use bounded verified chunks, Supervisor correlation/path rejection, backend publication and ProductEntry ref-only submission.
  - c2_backend_transfer_green: PASS — 15/15 importer/transfer/ProductEntry/persistence tests prove exact project-scoped raw publication, chunk/final identity rejection without publication, RuntimeSession correlation stripping and restart persistence; 21/21 backend transport tests also pass.
  - c2_adjacent_artifact_regression: PASS — 36/36 WS-C Artifact Store/policy/publication/reachability tests.
  - c2_adjacent_product_runtime_regression: PASS — 55/55 Product Runtime tests; exit 0.
  - c2_local_partition_ceiling: PARTIAL PASS — the former 16 MiB single-payload rejection is removed. Deterministic canonical chunks are bounded to <=8 MiB, manifest-addressed, and persisted as multiple Snapshot partitions; a forced-small-bound acceptance proves multi-partition publication and restart metadata. The default 2,000,000-row loop boundary is preserved but an actual 2,000,000-row resource benchmark remains NOT_RUN, so maximum-capacity evidence is not claimed.
  - c2_product_home_version_red: EXPECTED FAIL — Electron Supervisor silently forced expected_api_version 1.0 for the new 1.1 ProductEntry import; focused acceptance captured the exact outgoing contract/API options before the correction.
  - c2_product_home_green: PASS — additive ProductEntryService.v1.getProjectHome 1.1, strict Electron adapter, typed IPC/preload, current-context enforcement, date coverage and restart readback are connected. ProductEntry preserves the original three 1.0 operations; registry/fixture/manifest/seed now conform at 18 services/69 operations.
  - c2_renderer_data_green: PASS — 18/18 focused renderer tests prove chooser cancellation creates no Task, successful import adopts the returned ProjectContextRevision before getProjectHome, renderer state contains neither path nor bytes, reset refreshes canonical Data without reimport, and Data navigation is enabled only by the real connected bridge.
  - c2_focused_environment_retry: INFRA FAIL then PASS — the first focused runtime invocation pointed TEMP/TMP to a missing D-drive parent and 21 tests failed before assertions with ENOENT; after creating only that D-drive directory, the unchanged runtime set passed except a test-only noncanonical ID, then ProductBridge passed 22/22 after correcting the fixture ID.
  - c2_data_ui_build: PASS — typecheck and production build pass at 32 renderer modules; dirty BuildManifest bmanifest_sha256_145ce4ecc68932f738b7936bca0be41cc0586cec6396d154d13b444dc9ed45b7 is local evidence only.
  - c2_snapshot_validation_truth: PASS — seven blocking CLOSED_SCHEMA/UNIT_NORMALIZATION/UNIQUE_SYMBOL_DATE/OHLC_INVARIANTS/RESOURCE_BOUNDS/DATE_ORDERING/INSTRUMENT_RESOLUTION requirements and PASS results reference the format-independent canonical manifest; the Catalog transition now executes CANDIDATE→VALIDATED→PUBLISHED and CSV/Parquet replay remains immutable.
  - c2_multiload_boundary: PRESERVED — the frozen SQLiteA1CanonicalOwnerRepository remains a single-payload formal owner and was not reinterpreted as manifest-aware. Its exploratory edit was fully reverted (zero diff). V1.1 Factor must add a P1-verified manifest-aware reader instead of feeding manifest bytes to the legacy decoder.
  - c2_factor_panel_red: EXPECTED FAIL — `py -3.14 -B -m unittest apps.backend.tests.v1_1_factor.test_factor_panel -v` fails at import because DeterministicPanelEvaluator is absent. The new acceptance fixes actual manifest/partition identity, per-instrument rolling isolation, explicit DIVIDE_BY_ZERO_OR_MISSING, Golden MJ/MA/CROSS dates, per-date RANK and future-read rejection.
  - c2_factor_panel_green: PASS — unchanged focused acceptance 2/2. The manifest-aware reader verifies project reachability, PUBLISHED Snapshot/Universe, all seven blocking validations, actual manifest/membership/partition bytes and descriptor hashes before exposing a canonical panel. The additive axis registry and evaluator keep legacy hashes unchanged; legacy TDX/Factor regression executed 33 tests PASS with one pre-existing optional TA-Lib skip.
  - c2_factor_analysis_red: EXPECTED FAIL — `py -3.14 -B -m unittest apps.backend.tests.v1_1_factor.test_factor_analysis -v` fails at import because the FactorAnalysis owner module is absent; assertions fix one-member honesty and daily-before-aggregate statistical semantics.
  - c2_factor_analysis_green: PASS — unchanged 2-test acceptance after correcting the aggregate reason for a one-member Universe. The owner computes forward t→t+5 returns, Pearson/Spearman/quintiles per date before aggregation, stable tie splitting diagnostics, first-valid-day turnover unavailability and population IC std/ICIR over 23 valid golden dates.
  - c2_product_factor_persistence_red: EXPECTED FAIL — focused persistence test fails at import because ProductFactorStudyService is absent. It fixes exact user source persistence, six FactorDefinition/materialization owners, analysis artifact/read model and restart readback without recomputation.
  - c2_product_factor_persistence_green: PASS — unchanged focused acceptance 1/1. The service verifies ProjectContext/Snapshot/Universe ownership and bytes, uses the registered SHARES→TDX HANDS profile, evaluates six real named outputs, publishes <=8 MiB materialization partitions plus manifests, analysis and read model artifacts, persists six exact catalog definition/version pairs, and returns the identical JSON value model after restart. A discovered global-index warmup diagnostic defect was corrected to use each instrument's local position; no assertion was weakened.
  - c2_product_factor_entry_red: EXPECTED FAIL — the new durable acceptance initially failed because ProductEntryService.v1.submitFactorStudy was absent/unknown; the assertion fixed Task-before-Factor publication, closed intent and restart readback before implementation.
  - c2_product_factor_entry_green: PASS — submitFactorStudy now accepts only bounded formula source/output name, creates a durable QUEUED Task before Factor artifacts, dispatches isolated FACTOR_STUDY work, and recovers the exact formula/result through a restarted ProductRuntime. Original ProductEntry 1.0 operations remain the exact ordered prefix.
  - c2_factor_contract_regression: PASS — regenerated executable contract schemas/hashes and idempotent generator synchronization; contract/Data/Factor focus is 38 PASS plus 3 subtests at 18 services/70 operations.
  - c2_factor_electron_green: PASS — ProductBridge 41/41, renderer scope/navigation 20/20 and desktop bridge 23/23. The 22-channel frozen IPC surface rejects extra fields and caller owner IDs; main owns idempotency; canonical Factor readback rejects identity drift, non-finite values, incomplete output coverage and malformed exclusion tuples; Project A late completion is dropped after Project B activation.
  - c2_factor_ui_build: PASS — TypeScript typecheck and production build pass at 33 renderer modules. BuildManifest bmanifest_sha256_145ce4ecc68932f738b7936bca0be41cc0586cec6396d154d13b444dc9ed45b7 remains DIRTY local evidence only. One transient EPERM unlink during a combined build/test invocation was followed by an unchanged successful standalone build and is not promoted to a product defect.
  - c2_factor_context_recovery_red_green: EXPECTED FAIL then PASS — a direct restart regression proved get_latest_factor_study previously ignored ProjectContextRevision. Recovery now requires the exact project/context/snapshot triple; old-context Factor cannot leak into a newer current context.
  - c2_factor_link_recovery_green: PASS — restart readback verifies project-reachable PUBLISHED source manifest, FormulaDocument, every FactorDefinition/materialization manifest/partition descriptor, and analysis artifact with actual small-owner bytes and cross-artifact identities. Releasing the analysis reference leaves Data AVAILABLE but returns Factor UNAVAILABLE / FACTOR_READ_MODEL_NOT_AVAILABLE with no stale Factor payload.
  - c2_panel_property_green: PASS — 11 rolling-window subtests prove independent per-instrument warmup and perturbation isolation across MA windows 2..12; focused panel suite is 3 PASS + 11 subtests.
  - c2_required_product_smokes: PASS — npm.cmd run smoke:product-data and smoke:product-factor execute from D-drive temp storage. Data proves canonical restart identities; Factor proves durable QUEUED ProductEntry Task, real Golden formula/materialization, single-member INSUFFICIENT_SAMPLE and restart readback. No fixture substitution or production-availability claim.
  - c2_backend_current_aggregate: PASS — contract/Data/Factor focus 39 PASS + 14 subtests after recovery hardening.
  - c2_full_inherited_validation_before_latest_corrections: PASS — backend 39/39 suites, 70/70 files and 1030/1030 tests; unit 143/143; runtime 104/104; C1 faults 27 Python + 70 Electron; authority, typecheck, lint, 33-module build, bundle truth, dirty BuildManifest verification, frontend smoke, repo audit and SBOM all pass. Secret scan initially found only untracked Node compile caches under .codex-tmp; after exact temporary-directory removal it passed 723 source/history paths.
  - c2_format_to_factor_equivalence: PASS — one end-to-end acceptance imports the same panel as CSV and Parquet and proves identical normalized hash, Snapshot, Universe, FormulaDocument, all Factor output/materialization identities, FactorAnalysisResult and visual preview; distinct raw bytes remain distinct.
  - c2_provider_task_order_red_green: EXPECTED FAIL then PASS — the inline Product Research path previously captured the provider before creating any Task. It now durably accepts an inline Task/Run/Attempt, transitions to RUNNING, then captures; deterministic network unavailability returns exact PROVIDER_ACQUISITION_UNAVAILABLE with fallback_used=false/canonical_chain_created=false, leaves no RawCapture/Result, and persists Task FAILED plus the failure event.
  - c2_data_context_and_link_recovery: PASS — ProductDataService restart readback requires the exact project/context/snapshot triple and validates every read-model Artifact identity, semantic/reference role and active project reachability. Releasing the raw reference now fails closed instead of returning stale Data AVAILABLE; the focused local-data suite remains 16/16 PASS.
  - c2_latest_full_backend: PASS — after all provider/Data recovery corrections, the canonical backend inventory passed 39/39 suites, 70/70 files, 1031/1031 tests and compile gate.
  - c2_post_compaction_recovery: PASS — complete P0/request/attachment/2368-line plan/both-Ledger recovery; remote main/tree remain 02c5b8748170569ffc436f3bf5d2f682c21d2811/e3f3d3155177c17580015f4ef5b5405d0b689774, planned remote branch absent, 0 open PRs/issues, authority hashes match and P0 diff is empty.
  - c2_final_data_smoke_context_caller: FAIL — after authority/typecheck/lint/33-module build/bundle/manifest/runtime 104/104/C1 fault 27 Python + 70 Electron and Factor smoke passed, smoke:product-data failed because scripts/v1_1_product_data_smoke.py called the strengthened get_local_dataset without project_context_revision_id. Production exact-context behavior remains correct; the smoke caller must be corrected and rerun.
  - c2_final_data_smoke_context_caller_fix: PASS — only the smoke caller now supplies imported project_context_revision_id; Data smoke proves PRE_ALPHA/NOT_FORMAL canonical restart identities and the unchanged focused local-data suite passes 16/16.
  - acc_c2_01_secure_import: PASS — native chooser cancellation/non-regular/reparse/replacement race, bounded one-use transfer, CSV byte/row/instrument/UTF-8/closed-schema, nested Parquet, duplicate and OHLC negatives fail closed; invalid ProductData publication leaves Artifact/RawCapture/Snapshot counts at zero.
  - acc_c2_02_csv_parquet_equivalence: PASS — equivalent formats retain distinct raw identities but produce identical normalized hash, Snapshot/Universe, FormulaDocument, every Factor materialization/output, FactorAnalysis and visual preview.
  - acc_c2_03_unit_equivalence: PASS — SHARES and equivalent HANDS normalize to identical canonical rows/hash and identical per-row MJ; missing/unknown volume unit is rejected.
  - acc_c2_04_provider_failure: PASS — inline provider capture occurs only after durable Task/Run/Attempt acceptance; deterministic unavailability records exact PROVIDER_ACQUISITION_UNAVAILABLE, fallback_used=false, canonical_chain_created=false, Task FAILED/event evidence, and no RawCapture/Result/canonical bars.
  - acc_c2_05_golden_tdx: PASS — the real parser/translator/evaluator computes MJ, MA5/20/60 and both crosses against hand-calculated rows/dates with deterministic identities; no renderer formula fixture path exists.
  - acc_c2_06_axis_safety: PASS — per-instrument warmup/rolling isolation is property-tested over windows 2..12, RANK is per-date cross-section, and LEAD/negative lag/future reads fail closed.
  - acc_c2_07_single_symbol_honesty: PASS — price/MA/cross overlay remains available, while IC/RankIC/quantile metrics are INSUFFICIENT_SAMPLE with reason and no fabricated numeric cross-sectional chart.
  - acc_c2_08_cross_sectional_metrics: PASS — 20-symbol x 25-evaluable-date independent reference proves daily Pearson/Spearman/quintiles before aggregate mean/std/ICIR, coverage/missing/constant/tie/no-prior turnover branches.
  - acc_c2_09_persistence: PASS — Project/source/Snapshot/Universe/Formula/Factor/Analysis read models survive backend and Electron-shaped restart by exact canonical IDs, exact ProjectContext and active linked Artifact reachability without reimport/recompute.
  - acc_c2_10_product_ui: PASS — PRODUCT bundle contains no fixture/demo identifiers; enabled Data/Research actions issue real native bridge/ProductEntry durable Tasks and canonical Project Home readback; user-facing forms do not request owner IDs and trace IDs remain in lineage/details surfaces.
  - c2_live_provider_acceptance: NOT_RUN — no concrete post-failure availability-change signal exists; REAL_NETWORK_REQUEST_COUNT=0, no probe, provider switch or fallback was attempted.
  - c2_capacity_package_clean_machine: NOT_RUN — actual 2,000,000-row resource benchmark, packaged PyArrow/build-size delta, installer and distinct clean-machine journey remain deferred to their named later evidence gates; no stronger claim is made.
  - c3_strategy_authoring_environment_precheck: INFRA FAIL — the first focused invocation omitted the repository backend source path and failed on v3_backend import before reaching the new acceptance. The corrected command below is the acceptance evidence.
  - c3_strategy_authoring_red: EXPECTED FAIL — D-drive TEMP/TMP/PYTHONPYCACHEPREFIX; PYTHONPATH=apps/backend/src;.; `py -3.14 -B -m unittest apps.backend.tests.v1_1_strategy.test_product_strategy_authoring -v` fails only because `v3_backend.runtime.product_strategy` is absent. The test already builds actual C2 local Snapshot and boolean Factor study after the import boundary, requires exact entry/exit materialization identities, canonical Strategy/Portfolio/Risk decision-chain ID classes, PRE_ALPHA/NOT_FORMAL readback and restart persistence, and forbids caller weights/bars/factor values.
  - c2_checkpoint_write_set: PASS — staged diff contains 70 bounded C2 files, 13,088 insertions and 340 deletions; no cache/temp/P0 file is staged and cached diff --check passes.
  - clean-code-guard: 0 fixed, 7 flagged for author — FactorAnalysisService.analyze, ProductData _persist_snapshot/_persist_normalized_import/get_local_dataset, ManifestAwareLocalSnapshotReader.resolve, ProductFactor run_factor_study/_verify_recovered_study_links exceed the general function-size target. They remain cohesive single-owner seams and are explicitly non-blocking under this plan's no-large-refactor rule; no swallowed error, mock success, second evaluator, dead dependency or unverified boundary API was found.
  - test-guard: clean — C2 tests assert caller-visible owner bytes/IDs/states, use real SQLite/artifact files and real domain value objects, and mock only OS/runtime/provider/time boundaries. No internal call-count, framework-guarantee or fixture-success test was accepted.
  - docs-guard: clean — Ledger operation names, commands, counts, truth levels and NOT_RUN boundaries were checked against current contract registry/package scripts/source/test outputs. No packaged, provider, formal or production claim was promoted.
- git:
  - admitted_base_sha: 02c5b8748170569ffc436f3bf5d2f682c21d2811
  - admitted_base_tree: e3f3d3155177c17580015f4ef5b5405d0b689774
  - local_head_sha: ffb441590e1efb442ec8c1c20f68e74004ae177c
  - local_head_tree: b07ab828634bd71f8232d0265f12b9cf35eb4eb8
  - remote_head_sha: NOT_CREATED
  - branch: codex/v1-1-usable-research-product-01
  - status: DIRTY_C4_LOCAL_CANDIDATE_37_TRACKED_6_UNTRACKED
- github:
  - pr: NOT_CREATED
  - state: NOT_CREATED
  - checks: NOT_RUN
  - independent_review: PENDING
- blockers: NONE
- review_required: RESOLVED_BY_EXPLICIT_USER_AUTHORIZATION — Job F now records `BLOCKED_PROVIDER_ACCEPTANCE` only when product logs carry the exact `PROVIDER_ACQUISITION_UNAVAILABLE` code. An outer 900-second process timeout writes `FAIL_PROVIDER_ACCEPTANCE_TIMEOUT`; every other nonzero exit writes `FAIL_PROVIDER_ACCEPTANCE`. Broad `upstream/timed out/timeout` text can no longer downgrade an internal product failure to an upstream block.
- source_audit_finding: FINAL_LOCAL_GOAL_AND_EFFECT_AUDIT_PASS_AT_RECORDED_CEILINGS. C1 resilience, C2 Data/Factor, C3 Strategy/Backtest/Result and C4 Product Home/package/workflow/docs form one actual owner-resolved Product Entry journey; local source and same-machine packaged A/B effects match the Usable Research Product goal without a fixture or Formal-truth substitution. The final audit traced renderer gating, Main bridge validation, backend canonical owners, Task/worker/result persistence, restart discovery, exact-package transfer design and Job F classification rather than inferring correctness from aggregate tests. Local candidate is eligible to be presented for push authorization, not eligible to be called C4-complete or released. Hosted Jobs A-F, a CLEAN BuildManifest/release manifest, distinct clean-machine execution, real network acquisition, physical Windows scaling, user visual acceptance, PR and independent review remain literal `NOT_RUN` / `PENDING`; no production claim is made.
- compaction_recovery: PASS at 2026-08-25 12:32:03 +08:00. This continuation recovered the same goal/progress/authority and exact D-drive worktree after compaction, recomputed all authority hashes, verified zero P0 drift, and refreshed GitHub CURRENT before continuing only the recorded documentation/final-audit action. Exact branch/head/tree remain `codex/v1-1-usable-research-product-01` / `ffb441590e1efb442ec8c1c20f68e74004ae177c` / `b07ab828634bd71f8232d0265f12b9cf35eb4eb8`; remote main/tree remain `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, planned remote branch is absent and open PRs are zero. The protected original dirty root was not mutated. Next action is only the explicit push decision; do not push automatically.

# PROJECT_AUTHORITY

- authority_version: 1.0.2
- authority_status: P0_PROJECT_AUTHORITY
- P0_AUTHORITY_AMENDMENT authorization: ABSENT
- P0 authority modification: FORBIDDEN
- V3_PROJECT_CONSTITUTION.md SHA-256: 92ff8049addd10c1ca7f6ca293007b254045f3f63bae53ddc626b761da5bd32b
- AGENTS.md SHA-256: cbe7d78e2eccbfd5254fd08b30a0b145dc7c37b60aa5eadbbf4649b490f5b385
- docs/architecture/V3_CANONICAL_ARCHITECTURE.md SHA-256: ca74dcd00d2d20ba106d962b2455254f8ee69807df09d20ff4984e20a362bc5b
- docs/status/V3_CAPABILITY_LEVELS.md SHA-256: 79ca5210a33f283332884a9a4268e08a093ffe2d4ea33fe97d20672d355a9266
- docs/status/V3_PROJECT_AUTHORITY_MANIFEST.json SHA-256: 3306f51f4d9b26577f092d53e3a5cdb319619e9e9a75c0b90203c87bd21c425a
- authority hash status: MATCH
- authority drift rule: STOP_FOR_REVIEW

## GIT_GITHUB_CURRENT

- repository: dongxuelian11/v3-quant-workbench
- refreshed_at: 2026-08-25 12:32:03 +08:00
- remote main: 02c5b8748170569ffc436f3bf5d2f682c21d2811
- remote main tree: e3f3d3155177c17580015f4ef5b5405d0b689774
- open PRs: 0
- open issues: 0
- planned remote branch: NOT_FOUND
- exact-main ci: SUCCESS, run 32626717592
- exact-main packaging-clean-machine-evidence: SUCCESS, run 32626717564
- implementation worktree: D:\V3OpenSource-worktrees\v1-1-usable-research-product-01
- original root: D:\V3OpenSource at fd451c83005e444f84161e0c905f853a2b36d5a3 with 28 pre-existing status entries; do not modify or clean

## EXECUTION_BOUNDARIES

- One branch, one eventual PR, four checkpoint commits.
- No reset, rebase, force push, amend, auto-merge, recursive correction branch, or administrative bypass.
- No push until C1-C4 implementation and final task-goal/effect audit are complete and the user has been shown push eligibility.
- No fixture/provider fallback may mint product truth.
- Single-symbol Golden Case A must report cross-sectional metrics as INSUFFICIENT_SAMPLE.
- Golden Case B owns per-date cross-sectional IC/RankIC/quantile metrics.
- Formal Backtest checkpoint/resume remains NOT_AVAILABLE unless its full contract is genuinely closed.
- P0 authority files are outside the write set.
- The obsolete workflowguard-v3-preflight skill was uninstalled by explicit user request and is not an execution authority.

## CHECKPOINT_STATUS

- C1 Product Shell + Runtime Truth: COMMITTED f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6
- ACC-C1-01 Binding failure matrix: PASS
- ACC-C1-02 Project isolation: PASS
- ACC-C1-03 Health timeout recovery: PASS
- ACC-C1-04 Exit fencing: PASS
- ACC-C1-05 Real cancellation: PASS
- ACC-C1-06 Deadline: PASS
- ACC-C1-07 Bounded memory: PASS
- ACC-C1-08 UI truth: PASS
- C1 section 11.5 control-plane composition re-audit: PASS
- C2 Data + Factor Research: COMMITTED 07cefa53e16ebed277a2bd1423f851379aa4732b; ACC-C2-01..10 PASS
- C3 Strategy + Backtest + Final Result: IN_PROGRESS; SOURCE_OWNER_AUDIT
- C4 Usability + Release Qualification: NOT_STARTED
- Final goal/effect re-audit: NOT_STARTED
- Commit: C1 f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6; C2 07cefa53e16ebed277a2bd1423f851379aa4732b
- Push: NOT_RUN
- PR: NOT_CREATED

## C1_SOURCE_EFFECT_REAUDIT

- 11.1 characterization: PASS — original failure modes remain represented by named regressions; no assertion was deleted to make them green.
- 11.2 correlated control: PASS — closed control envelope, generation fencing, bounded pending/tombstone ownership, health/shutdown/Product Entry correlation.
- 11.3 process exit: PASS — graceful ack, wait, terminate, kill, confirmed-exit fence; unconfirmed exit blocks the next generation.
- 11.4 atomic activation: PASS — BindingActivationV2 rollback matrix and renderer ProjectScopeToken drop late Project A responses after Project B activation.
- 11.5 task coordinator: PASS — existing TaskSupervisor/WorkerSupervisor composition, independent command/response pipes, child SQLite UoW, 2-second heartbeat, 10-second lease, real cancellation/deadline/restart reconciliation, lazy monitor lifecycle.
- 11.6 router/errors: PASS — bounded request cache, enforced deadline, Exception-only orchestration, unknown INTERNAL_ERROR nonretryable, touched bridge coercions fail closed.
- 11.7 pagination/persistence: PASS — scope/filter/sort-bound opaque keyset cursors, page size 1..100, explicit next-page UI, virtualized lists, ENOENT-only BindingStore default, bounded Workspace V2 backup/rollback migration.
- 11.8 UI truth: PASS — five fixed Chinese-first pages, only real Home enabled, four deferred pages disabled with NOT_AVAILABLE reasons, release bundle scanner rejects demo identifiers.
- 11.9 migration: PASS — additive transactionally validated 0005 adds deadlines, task_output, publication_intent, immutable session/project binding, and cursor indexes; old-catalog upgrade/backup failure paths are tested.
- 11.10 acceptance: PASS — ACC-C1-01..08 plus post-green source audit and all required commands.
- evidence ceiling: LOCAL_WORKTREE / SAME_MACHINE only. No clean-machine, packaged installer, hosted, PR, independent review, merge, or production claim is made for this checkpoint.
- deferred_to_C2: exact-main already contains admitted_truth_state DEMO, submitResearch truth_state DEMO, and sgv_placeholder. These were verified in both HEAD and working source, were not introduced by C1, and remain a mandatory C2 truth-chain correction rather than hidden debt.
- clean-code-guard: runtime error classification, partial-start exit fencing, worker configuration object, execution-path split, non-swallowing lease monitor, and idle-monitor lifecycle fixed; long OS child entrypoint remains cohesive and is not generalized further in C1.
- test-guard: clean — new tests inject at OS process/SQLite/lease boundaries and assert durable state plus real process exit, not internal call counts.
- docs-guard: claims above were checked against current symbols, migrations, package scripts, test inventories, Git status, and authority hashes; no stronger availability claim is recorded.

## C2_SOURCE_OWNER_AUDIT

- audit_basis: The GAP map below is the historical admission baseline from the post-compaction source audit. Current implementation results at lines above and the superseding delta below are execution truth; historical GAP text must not be used to repeat completed owners.
- ACC-C2-01 secure import: GAP — no LocalDataImportIntentV1, native chooser capability, bounded streaming CSV/Parquet importer, symlink/reparse/TOCTOU fence, or import security suite exists.
- ACC-C2-02 CSV/Parquet equivalence: GAP — the packaged Python lock contains pandas but no admitted Parquet engine; the existing Parquet port is explicitly unavailable and existing snapshot partitions do not contain canonical bar bytes.
- ACC-C2-03 unit equivalence: PARTIAL OWNER ONLY — TdxDataSemanticProfileVersion supports explicit canonical-to-TDX multipliers, but no local-import DataSemanticProfile or SHARES/HANDS MJ product gate exists.
- ACC-C2-04 provider failure: PARTIAL — ProductResearchService captures inside the C1 child worker after durable acceptance and records fallback_used=false/canonical_chain_created=false; current operation remains the legacy single-symbol profile and the connector capability row still uses the legacy DEMO vocabulary.
- ACC-C2-05 Golden TDX: PARTIAL — real TdxParser/TdxTranslator and canonical Factor IR are present, but admitted translation supports only MA/CROSS; Product Research computes only source close and constructs its score payload through a temporary sgv_placeholder reference. No Golden MJ/MA/cross product study exists.
- ACC-C2-06 axis safety: PARTIAL OWNER ONLY — the legacy deterministic evaluator is one-dimensional per series and rejects PIT-unsafe LEAD for admitted definitions, but there is no V1.1 panel evaluator owning instrument/date partitions or cross-section-per-date RANK.
- ACC-C2-07 single-symbol honesty: GAP — no FactorAnalysis application service or product read model returns INSUFFICIENT_SAMPLE for one-member Universe; Data and Research pages remain disabled.
- ACC-C2-08 cross-sectional metrics: GAP — there is no date-wise IC/RankIC/quantile/coverage/missing/turnover owner matching FactorAnalysisSpecV1.
- ACC-C2-09 persistence: PARTIAL PASS — local CSV/Parquet RawCapture, normalized Snapshot, USER_DEFINED_STATIC Universe, new ProjectContext and project-scoped Data read model now persist and survive restart with exact raw/normalized identities. Formula, V1.1 factor materialization and analysis persistence remain absent, so the full acceptance is not yet PASS.
- ACC-C2-10 Product UI: GAP — PRODUCT shell keeps Data/Research disabled and the Home panel exposes only legacy ProductEntryService.v1.submitResearch; no enabled V1.1 Data/Factor actions exist.
- canonical owner map: provider=AkshareAShareEodAdapter + ProviderRegistry + ProductResearchService child execution; raw/snapshot=normalize_a_share_eod + product_research Catalog persistence; Universe=product_research WATCHLIST/observed membership path; formula=TdxParser/TdxTranslator; Factor=FactorDefinitionVersion + DeterministicReferenceEvaluator + FeatureMaterialization/FactorEvaluation; UI=ProductApp/ProductRuntimePanel/productRuntimeStore; local import and FactorAnalysis owners are absent.
- first source-proven correction: remove the non-canonical bootstrap placeholder without changing legacy score-payload bytes. Add a binding-free encoder over exact StrategyDefinitionVersion + ExactUniverseReference, retain encode_score_payload as a compatible wrapper, and regress both payload equivalence and absence of placeholder source text.
- first source-proven correction result: PASS at local worktree source level — Product Research now encodes owner score bytes directly against the exact Universe reference; the legacy encode_score_payload entry point remains a compatibility wrapper; sgv_placeholder and the temporary binding are absent from production source.
- adjacent regression hardening: PASS — ACC-C1-06 no longer races a worker that has not entered the non-cooperative start delay; it waits for the real DISPATCHED response and still requires OS terminate evidence.
- local-data semantic owner result: MODULE_ACCEPTED for the bounded importer, additive persistence slice and durable ProductEntry 1.1 acceptance on the dirty C2 worktree — CSV reads through a bounded hashing RawIO stream; Parquet requires a bounded seekable handle, checks metadata/schema before `iter_batches`, rejects nested/dictionary/binary/extension columns, and normalizes canonical volume to SHARES. ProductDataService now proves durable raw Artifact/Snapshot/Universe/ProjectContext/read-model restart persistence and isolated Task execution. Native chooser/transfer security, product bridge/UI, >16 MiB chunked partitions, packaged PyArrow and clean-machine evidence remain NOT_RUN/NOT_AVAILABLE as stated.
- dependency owner result: PyArrow 23.0.1 is an exact direct-runtime requirement and SBOM entry for CPython 3.14 win_amd64. The official wheel identity is recorded, but package:win/clean-machine proof remains NOT_RUN and no PRODUCTION_AVAILABLE claim is made.
- superseding_current_delta: Secure native CSV/Parquet transfer, normalized Snapshot/Universe persistence, deterministic <=8 MiB partitions, seven blocking validations, Golden TDX panel, single-symbol honesty, cross-sectional analysis, durable Factor ProductEntry, canonical Project Home readback and connected Data/Research UI are locally green. The Product path is PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL only; it is not FORMAL or production available.
- truth ceiling: C2 remains IN_PROGRESS during post-green source/effect audit and complete validation. Actual 2,000,000-row resource evidence, packaged PyArrow/build-size proof, clean-machine evidence, PR review and production availability remain literal NOT_RUN/NOT_AVAILABLE.

## C3_SOURCE_OWNER_AUDIT

- audit_state: COMPLETE for pre-red design admission at 2026-08-24 15:15:55 +08:00. This is a source-owner/GAP map, not C3 implementation evidence. No C3 production module, test, commit, push or capability promotion exists yet.
- actual Data payload owner: ProductDataService persists immutable normalized manifest/partitions and ProjectContext-bound Snapshot/USER_DEFINED_STATIC Universe; ManifestAwareLocalSnapshotReader resolves and verifies actual project-reachable manifest, membership and partition bytes plus all seven blocking validations before exposing rows. ProductDataService.get_local_dataset returns a bounded read model, not authoritative rows.
- actual Factor payload owner: ProductFactorStudyService persists exact FactorDefinitionVersion plus content-addressed materialization manifests/partitions. get_latest_factor_study verifies project/context/snapshot identity and descriptor reachability, but it deliberately does not decode/verify every Factor partition byte. C3 therefore needs a bounded P1-verified boolean Factor materialization resolver over those existing bytes; it must not consume visual_preview or renderer values as truth and must not change C2 identities.
- Strategy semantic owner: StrategyDefinitionVersion/StrategyCompiler/DeterministicStrategyEvaluator and FormalStrategyEvaluationService already provide one-decision closed IR, actual owner-payload resolution, SignalArtifact, SelectionArtifact and PortfolioIntent. The accepted registry supports score binding, minimum gate, deterministic rank/top-N and equal desired exposure. It has no persistent entry/exit state and owns no orders/fills.
- Strategy design decision: ProductStrategyService will be a bounded Strategy-owner orchestrator around the existing compiler/evaluator, not a second financial engine. It resolves actual entry/exit boolean Factor partitions, validates identical project/Snapshot/Universe/context, and applies a deterministic content-addressed 0/1 state-transition reducer whose only responsibility is flat/long state and repeated-entry/repeated-exit diagnostics. Per decision, the existing canonical evaluator still mints Signal/Selection/PortfolioIntent from a resolver-backed Strategy state payload. State rows/receipts must bind previous state and exact entry/exit materialization hashes; visual_preview is forbidden as an input.
- Portfolio semantic owner: CanonicalPortfolioOwnerService plus DeterministicPortfolioConstruction remain the sole TargetWeight mint. Empty selection is admitted only by a pinned all-cash construction spec; active selection uses a separately content-addressed invested spec with target cash equal to 1-gross exposure. The bounded orchestrator selects between these two canonical specs from the verified state transition; it never accepts renderer/caller weights.
- Risk semantic owner: CanonicalRiskPolicyAuthoringService and CanonicalRiskApplicationService with SQLitePortfolioRiskPolicyOwner/SQLiteRiskApplicationRepository already persist exact PRE_ALPHA policy/application artifacts, re-resolve actual TargetWeight and fail closed. C3 reuses these owners and may author only the existing closed pass-through/max-single-name/gross/net policies.
- Backtest semantic owner: DeterministicAshareBacktestEngine and FormalBacktestService already own Decimal daily simulation, next-effective scheduled weights, sells-before-buys, T+1, lots, explicit costs, cash/position ledgers, raw-close NAV and admitted cash-dividend/integral split actions over resolved actual owner payloads. They are the only engine to extend.
- Result Analytics semantic owner: DeterministicResultAnalyticsEngine already derives total/annualized return, volatility, Sharpe, Sortino, drawdown, monthly/yearly returns, exact costs/turnover and explicit benchmark unavailability from a verified Result payload. Calmar, exposure/concentration, product-connected tables and full lineage remain absent.
- publication/finality owner: artifact publication currently cannot atomically cover filesystem and SQLite. Migration 0005 has an unused publication_intent state table and a narrow artifact-only task_output table. SQLiteResultRepository supports PENDING_RECONCILIATION then VALID/INVALID, but ProductExecutionService publishes bytes/Result separately, never finalizes reconciliation, and can mark Task success while Result remains pending. C3 must wire a durable recovery saga and exact Result finality; ordinary try/except compensation is insufficient.
- Product boundary: ProductBridge/contracts still expose only synchronous legacy submitExistingBacktestRunSpec, summary-only getResult and ticket-only openArtifactStream. Product navigation keeps Backtest/Results disabled with explicit C3 reasons. Legacy Strategy/Backtest/Result panels and development Result fixtures are not product truth and cannot be enabled.
- ACC-C3-01 async contract: GAP — publishResearchStrategy/submitResearchBacktest operations, durable work kinds and C3 product read models do not exist; the C1 worker coordinator is reusable.
- ACC-C3-02 signal timing: GAP — no verified entry/exit state schedule or t-close to next-admitted-open Product path exists; existing ScheduledWeights can represent effective times once canonical vectors exist.
- ACC-C3-03 fill realism: GAP — existing engine fills eligible requests fully, lacks slippage/volume participation and uses linear lot decrement for affordability.
- ACC-C3-04 rule truth: GAP/PARTIAL OWNER — FormalBacktestService can build typed market state from verified canonical status owners, but C2 local rows contain explicit UNKNOWN fields and no STRICT/RESEARCH_APPROXIMATE resolver or AssumptionReceipt path exists. Caller flags must remain non-authoritative.
- ACC-C3-05 initial holding: GAP — BacktestRunSpec.create does not reject acquired_on after first session; T+1 core behavior otherwise exists.
- ACC-C3-06 corporate actions: PARTIAL — cash dividend and integral bonus/split exist; fractional and unsupported types fail during execution rather than the required preflight, and no delisting/rights coverage gate exists.
- ACC-C3-07 publication crash matrix: GAP — publication_intent schema exists but has no production recovery service or fault matrix.
- ACC-C3-08 reconciliation corruption: GAP/PARTIAL OWNER — Result repository can record reconciliation state, but no product ledger reconciler/finalizer verifies sequence, fees, cash, positions, NAV, reachability and terminal Task coupling.
- ACC-C3-09 stream security: GAP — current API creates an in-memory ticket only; no consume/chunk/complete/close path, one-use registry, generation fence or complete current-project reachability proof exists.
- ACC-C3-10 export truth: GAP — current export creates a manifest/plan only and does not write verified destination bytes.
- ACC-C3-11 analytics: PARTIAL — existing deterministic metrics are real; Calmar, exposure, concentration and product sections are missing.
- ACC-C3-12 lineage: GAP/PARTIAL OWNER — individual canonical owners preserve refs, but no finalized Result read model proves the complete Factor to Signal to TargetWeight/Risk to Order/Fill to Result chain and actual-byte reachability.
- ACC-C3-13 restart: GAP — no C3 Strategy/VALID Result product read model exists; renderer C3 pages remain disabled.
- first bounded implementation sequence: (1) red/green ResearchStrategySpecV1 authoring and actual Factor-row resolver; (2) durable ProductEntry publishResearchStrategy plus restart readback; (3) engine extensions and research RunSpec/worker path; (4) publication/reconciliation finality; (5) stream/export/analytics; (6) bridge/renderer and full C3 acceptance. Each step reuses existing owners and preserves C2 artifacts/hashes.
- truth ceiling: C3 remains IN_PROGRESS / SOURCE_AUDIT_COMPLETE only. Strategy/Backtest/Result product capability is still NOT_AVAILABLE; no test or module result has been promoted.

## C3_CURRENT_SOURCE_EFFECT_MATRIX

This matrix supersedes the historical pre-red GAP map above. It is based on current production owners, their actual-byte/restart paths, and user-visible renderer effects; green aggregate tests alone are not treated as sufficient evidence.

### Plan sections 14.1-14.9

| Section | Current state | Source/effect conclusion |
|---|---|---|
| 14.1 Strategy authoring | PASS at LOCAL_WORKTREE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL | Exact Factor partitions are re-resolved, existing Strategy/Portfolio/Risk owners mint the decision chain, preview is side-effect-free, Publish is durable and restart/tamper/profile-drift fail closed. |
| 14.2 Backtest preflight | PASS for no-action and bounded admitted-action scope | Project/context/Strategy/Data/policy/date/resource bindings fail before Task acceptance; lightweight action-set refs resolve through a project-scoped Data Truth owner while exact row/event bytes remain worker-resolved. Missing/unsupported/fractional action truth fails before Task creation. |
| 14.3 Engine repair | PASS for the research profile | The sole existing A-share engine owns next-open timing, T+1, future-holding rejection, adverse slippage, volume partial fills, O(log lots) affordability, explicit diagnostics and identity changes. |
| 14.4 Execution policy coverage | PASS for the published bounded matrix | Product Home and preflight share one backend-owned coverage registry; uncovered ranges fail closed and no adjacent policy is extrapolated. Broader historical/board coverage remains NOT_AVAILABLE. |
| 14.5 Result publication/reconciliation | PASS for the bounded V1.1 operation set | ProductEntry research Backtest uses a durable six-state publication saga, deterministic reconciliation and atomic VALID/Task-success coupling. `ResultService.v1.reconcileLedger`, `finalizeResult` and all seven `getResult` sections re-verify the current ProjectContext, sole VALID Result, FINALIZED intent and actual owner bytes in main preflight and isolated execution. Comparison remains explicitly `RESULT_COMPARISON_NOT_AVAILABLE`, as permitted by 14.5; no formal truth promotion is claimed. |
| 14.6 Result analytics | PASS for V1.1 scope | Deterministic analytics includes return/volatility/Sharpe/Sortino/drawdown/Calmar, monthly/yearly, cost/turnover, exposure/concentration, exact source bindings and BENCHMARK_NOT_AVAILABLE. |
| 14.7 Artifact stream/export | PASS at LOCAL_WORKTREE transport scope | Project reachability, one-use/expiry/generation fencing, per-chunk/full hash verification and real native tmp/fsync/hash/atomic rename are connected. Summary JSON, orders CSV, fills CSV and analytics JSON originate only from revalidated VALID Result refs; failure deletes the unique tmp before the durable FAILED receipt. Sink writes are serialized, but no stronger packaged or clean-machine throughput claim is made. |
| 14.8 Task feedback | PASS for retry-from-start scope | Durable bounded phases render without fake percentages. Persisted Product Backtest failures expose exact category/Attempt details, and only the five backend RetryPolicy-admitted categories show 从头重试. Electron re-reads Task truth, backend preserves immutable Task/Run with a new isolated Attempt, and renderer rebuilds only canonical VALID Home/Result readback. Change-source/import actions remain on the Data workspace rather than becoming implicit fallback. |
| 14.9 Strategy/Backtest/Result UI | PASS for latest VALID bounded result, comparison UNAVAILABLE | Backend-owned Strategy/Snapshot/Universe, allowed range, cash, commission, slippage, participation, assumption mode and resource estimate are visible; only an exact matching PASS/NONE preflight enables Run and submit re-preflights. Results opens only latest VALID and rebuilds metrics, charts, orders, fills, explicit fee components, positions, diagnostics, period returns and lineage from exact artifacts. PENDING/INVALID never enter the VALID view; comparison remains explicitly unavailable. |

### ACC-C3-01..13

| Acceptance | Current state | Evidence ceiling / remaining gap |
|---|---|---|
| ACC-C3-01 Async contract | PASS at LOCAL_WORKTREE scope | ProductEntry submit returns durable QUEUED before owner execution; a direct correlated RuntimeSession health/getTask/getEvents batch remains responsive while the exact Product Backtest child is live. |
| ACC-C3-02 Signal timing | PASS | Close-t signals execute only at the next admitted open; same-bar lookahead is rejected. |
| ACC-C3-03 Fill realism | PASS | Partial fill, adverse buy/sell slippage, cash non-negativity and bounded affordability are source- and test-proven. |
| ACC-C3-04 Rule truth | PASS for STRICT/APPROXIMATE admitted scope | Resolver-owned state controls execution; STRICT unknown blocks and APPROXIMATE emits AssumptionReceipt/identity. Caller booleans cannot override owner payloads. |
| ACC-C3-05 Initial holding | PASS | Future acquired_on fails RunSpec preflight; legal T+1 behavior remains green. |
| ACC-C3-06 Corporate actions | PASS at LOCAL_WORKTREE / PRE_ALPHA / NOT_FORMAL scope | Product publishes and re-resolves actual content-addressed action-set bytes under an exact Project owner reference, binds each event to its Snapshot row, carries the exact cax input into RunSpec and proves positive cash-dividend/integral-split ledger effects. Fractional, rights, delisting, OTHER and missing owners fail closed before Task acceptance; no formal or packaged claim is made. |
| ACC-C3-07 Publication crash matrix | PASS | Six named crash boundaries converge idempotently to exactly FINALIZED or FAILED; no Task success with non-VALID Result. |
| ACC-C3-08 Reconciliation corruption | PASS | Sequence/cash/fee/position/NAV corruptions produce INVALID Result, FAILED Task and durable failure reconciliation evidence. |
| ACC-C3-09 Stream security | PASS at LOCAL_WORKTREE transport scope | Cross-project/expiry/replay/generation/hash failures reject; large cross-language byte reassembly is exact. |
| ACC-C3-10 Export truth | PASS at LOCAL_WORKTREE transport scope | Cancel is NOT_RUN; accepted export completes only after exact native write/hash/fsync/rename; failure removes the unique temporary file and records a FAILED Task without publishing a manifest. |
| ACC-C3-11 Analytics golden | PASS | Independent fixtures cover all named metrics and explicit unavailable states. |
| ACC-C3-12 Result lineage | PASS | Restart readback re-resolves and verifies RawCapture through Result/Analytics plus decision/order/fill projection; semantically tampered lineage rejects. |
| ACC-C3-13 Restart | PASS | Latest VALID Home/result artifacts rebuild after backend/Electron-shaped restart without recomputation or renderer cache. |

C3 checkpoint eligibility: ELIGIBLE_FOR_LOCAL_CHECKPOINT_COMMIT. Plan 14.1..14.9 and ACC-C3-01..13 are locally closed at the recorded PRODUCT_CONNECTED / PRE_ALPHA / RESEARCH_ONLY / NOT_FORMAL ceilings, including ResultService bounded bindings, Product Corporate Action owner/resolution, current-diff full validation and all required product smokes. Packaged product, clean machine, remote CI, PR, independent review and production availability remain NOT_RUN/PENDING and are C4/final-PR concerns; no push is authorized by this eligibility statement.

## C4_CURRENT_SOURCE_EFFECT_MATRIX

This matrix is the fresh C4 admission audit after checkpoint 3 commit `ffb441590e1efb442ec8c1c20f68e74004ae177c`. It records source and observable-effect truth before C4 production edits; prior V1.0 packaging evidence is not promoted to V1.1 evidence.

### Plan sections 15.1-15.9

| Section | Current state | Source/effect conclusion |
|---|---|---|
| 15.1 Information architecture | PASS at LOCAL_WORKTREE plus PACKAGED_SAME_MACHINE / EMULATED_ELECTRON_ZOOM scope | `ProductApp` has exactly the five required Chinese-first pages. Home now derives its current project, recent Data/Factor/Strategy/VALID Result timeline, project switching and single next action only from `ProductProjectHomeView`; legacy B3 Product Research/RunSpec and BuildManifest diagnostics are folded outside the V1.1 Golden Journey. Exact-current `visual-probe-9-home-final` proves 60 screenshots/12 Home cases with correct `PROJECT_BOUND`, no horizontal scroll/clipped interactions/unnamed control and actual keyboard navigation. Physical Windows scaling and user visual acceptance remain NOT_RUN/PENDING. |
| 15.2 Visual and interaction gate | PARTIAL PASS at PACKAGED_SAME_MACHINE / EMULATED_ELECTRON_ZOOM | Final `visual-probe-6-final` closes the 4×3 Electron zoom matrix over all five real Product pages, machine layout/typography/labels/status/chart alternatives and nav Tab/Return/focus visibility. It does not prove physical Windows 100/125/150 scaling, a keyboard-complete native file chooser Golden Journey, every empty/loading/error/disconnected visual state, or `USER_VISUAL_ACCEPTED`; those remain literal NOT_RUN/PENDING. |
| 15.3 Golden Journey A | PASS_CANDIDATE at PACKAGED_SAME_MACHINE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL | The real unpacked product starts from empty isolated userData, imports user-selected local CSV bytes, publishes the Golden Factor and bounded Strategy, executes the V1.1 Backtest to a VALID Result with orders/fills/analytics/lineage, then a new packaged renderer/backend process reconstructs the exact canonical identities without submission or known-ID injection. Distinct clean-machine and hosted evidence remain NOT_RUN. |
| 15.4 Golden Journey B | PASS_CANDIDATE at PACKAGED_SAME_MACHINE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL | The real unpacked product imports the 20-symbol panel, displays the owner-derived Factor Analysis, matches an independently computed sample/IC/Rank IC/quantiles/spread reference, and a new packaged renderer/backend process reconstructs the exact Factor identities and values without recomputation or known-ID injection. Distinct clean-machine and hosted evidence remain NOT_RUN. |
| 15.5 Unified CI product gate | SOURCE PASS / HOSTED NOT_RUN | Explicitly authorized exact-head Jobs A-C and exact-package Jobs D-F are wired. Every Action invocation in the repository is pinned to one audited 40-hex SHA; five workflow contracts, YAML, PowerShell parsing and exact-current local validation pass. A remote task head does not exist, so hosted Jobs A-F remain NOT_RUN. |
| 15.6 Clean-machine truth | SOURCE PASS / DISTINCT RUNNER NOT_RUN | Job E has no checkout and downloads only Job D's delivery. The transferred standalone driver verifies package/installer/release/runtime hashes, installs, runs A/B plus restart through packaged Electron Node without source/npm/pip/system Node, checks storage isolation, install-tree immutability, graceful exit/orphans and uninstalls. Local source/parser/package-driver effects pass; distinct-runner execution remains NOT_RUN. |
| 15.7 Documentation truth reconciliation | GAP | README and release scripts describe V1.0; `CURRENT_STATUS` still says Desktop/backend and Backtest/Result were not rebuilt; `PRODUCT_SURFACE` says Backtest/Result backend unavailable; Deferred Gaps retain now-stale runtime and Result Analytics entries; the V1.0 release document still says exact-head CI is pending. Non-P0 docs require exact-candidate reconciliation without upgrading formal/provider/hosted truth. |
| 15.8 Version and package | LOCAL PACKAGE PASS_CANDIDATE / RELEASE GATED | Version and installer names intentionally remain 1.0.0. Exact-current unpacked package verification and A/B same-machine effects pass, while `verify:release` correctly rejects the DIRTY BuildManifest. A 1.1.0 bump and release qualification remain gated on the remaining C4 documentation/final-review boundary and later clean exact-head evidence. |
| 15.9 C4 acceptance | IN_PROGRESS | Local implementation, unified workflow source and exact-current same-machine package effects pass. Documentation reconciliation remains the next local gate. Hosted exact-head Jobs, distinct clean-machine, live provider truth, clean release identity and independent review require later remote evidence and remain literal NOT_RUN/PENDING. |

### ACC-C4-01..11

| Acceptance | Current state | Exact remaining evidence |
|---|---|---|
| ACC-C4-01 Journey A | PASS_CANDIDATE at PACKAGED_SAME_MACHINE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL | Exact `3LIkjN` evidence proves the complete real Product V1.1 local-import → Factor → Strategy → Backtest → VALID Result chain. Distinct clean-machine and hosted evidence remain NOT_RUN. |
| ACC-C4-02 Journey B | PASS_CANDIDATE at PACKAGED_SAME_MACHINE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL | Exact `3LIkjN` evidence proves the packaged 20-symbol Factor Analysis and exact independent-reference match. Distinct clean-machine and hosted evidence remain NOT_RUN. |
| ACC-C4-03 Restart | PASS_CANDIDATE at PACKAGED_SAME_MACHINE / PRODUCT_CONNECTED / PRE_ALPHA / NOT_FORMAL | A and B each close the first process and use a new packaged renderer/backend process to discover and reconstruct the exact canonical chain without known-ID injection; all backend PIDs terminate and the install tree is unchanged. Distinct clean-machine evidence remains NOT_RUN. |
| ACC-C4-04 Screen matrix | PARTIAL PASS at PACKAGED_SAME_MACHINE / EMULATED_ELECTRON_ZOOM | 60/60 Product page screenshots and machine audits pass, with representative 1366×768@150 images visually inspected. Physical Windows scaling and full state-board/user acceptance remain NOT_RUN/PENDING, so ACC-C4-04 is not promoted to complete PASS. |
| ACC-C4-05 Accessibility baseline | PARTIAL PASS at PACKAGED_SAME_MACHINE | Nav keyboard activation/focus visibility, form labels, textual status, chart summaries/titles and table alternatives pass. The complete Golden Journey through the native file chooser and user accessibility acceptance remain NOT_RUN/PENDING. |
| ACC-C4-06 Product truth scan | PASS only for the last LOCAL_WORKTREE checkpoint | Current `validate:public` bundle truth and connected bridge tests pass; exact packaged C4 candidate evidence is NOT_RUN. |
| ACC-C4-07 Unified CI | SOURCE PASS / HOSTED NOT_RUN | The authorized workflow implementation exposes exact-head Jobs A-C and exact-package Jobs D-F with all repository Actions pinned to audited immutable SHAs. Source/YAML/embedded-shell contracts and local validation pass; exact remote-head execution is NOT_RUN. |
| ACC-C4-08 Clean machine | SOURCE PASS / DISTINCT RUNNER NOT_RUN | The approved Job E wiring has no checkout and executes only the transferred package/installer/manifests/drivers. The standalone verifier's source/parser contracts pass and the same V1.1 package driver passes locally; it is not promoted until a distinct hosted no-checkout/no-install runner executes it. |
| ACC-C4-09 Package integrity | PASS_CANDIDATE at PACKAGED_SAME_MACHINE / RELEASE NOT_RUN | Current exact-worktree package verification records 1,049,994,307 bytes / 13,904 files / SHA-256 `973e8b78c31320f8d26893104922df6c0da11e74bd8e20d8fc5325233db01932`, embedded CPython 3.14.5, 52 dependencies and AKShare 1.18.84. A/B and restart pass with an unchanged copied install tree. The NSIS installer exists, but `verify:release` rejects the intentionally DIRTY BuildManifest; clean exact-head release and distinct transfer remain NOT_RUN, and version stays 1.0.0. |
| ACC-C4-10 Docs | GAP | Reconcile the named non-P0 documents against the exact candidate and retain evidence ceilings. |
| ACC-C4-11 Governance | PENDING | No remote branch or PR exists. Exact-head checks and non-author independent approval cannot be claimed before the user authorizes push/PR. |

C4 sequencing boundary: the plan text says C4 acceptance must all pass before checkpoint commit, while ACC-C4-07/08/11 require a remote exact commit, PR workflow and independent reviewer. The user also requires final source/effect review before considering push. Execution therefore prepares and validates the complete local C4 candidate without changing version or claiming C4 completion, presents push eligibility, and waits for explicit push authority. It will not manufacture a fifth commit, amend checkpoint 4, or upgrade remote evidence from local results.

## COMPACTION_RECOVERY

After any automatic compaction:

1. Read V3_PROJECT_CONSTITUTION.md completely.
2. Read docs/architecture/V3_CANONICAL_ARCHITECTURE.md completely.
3. Read docs/status/V3_CAPABILITY_LEVELS.md completely.
4. Read the original user requests and both audit attachments.
5. Read docs/release/V1_1_USABLE_RESEARCH_PRODUCT.md completely.
6. Read this Ledger from the first line.
7. Recompute all Authority Manifest hashes.
8. Refresh Git/GitHub CURRENT.
9. Verify worktree, branch, HEAD, dirty files, and remote state against this Ledger.
10. If any identity or authority drift exists, STOP_FOR_REVIEW.
11. Otherwise execute only exact_next_action; do not repeat completed work.

Latest recovery: PASS at 2026-08-24 after the current automatic compaction. The complete mandatory read sequence was replayed from the three P0 doctrine files through the original requests, both audit attachments, the 2368-line execution plan and both Ledgers. Authority Manifest 1.0.2 and all five recorded hashes match; `validate:authority` and P0 diff PASS. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; current dirty inventory is 46 collapsed / 62 expanded entries, accounted for by the bounded C3 source/test/UI delta. GitHub main/tree remain `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, with zero open PRs/issues, no remote program branch, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. Original root remains `fd451c83005e444f84161e0c905f853a2b36d5a3`; its now-observed 29 dirty entries are preserved and were not touched. Continue only with the focused Backtest preflight regression and policy-coverage owner audit; push remains NOT_RUN.

Current recovery: PASS at 2026-08-24 after the newest automatic compaction. The complete P0 authority set, AGENTS, original user requests, both audit attachments, all 2368 plan lines and both Ledgers were reread before task action. Authority Manifest 1.0.2 and every recorded SHA-256 match; `npm.cmd run validate:authority` PASS and the P0 diff is empty. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; the current bounded C3 inventory is 59 collapsed / 63 expanded files = 47 tracked + 16 untracked. GitHub CURRENT remains main `02c5b8748170569ffc436f3bf5d2f682c21d2811`, zero open PRs/issues, remote program branch absent, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. The preserved original root remains at `fd451c83005e444f84161e0c905f853a2b36d5a3` with 28 currently observed dirty entries; no root mutation was made. Continue only with the source-proven legacy BacktestService capability correction and affected gates; push remains NOT_RUN.

Current recovery: PASS at 2026-08-25 after automatic compaction and two system turn interruptions. The complete Constitution, Canonical Architecture, Capability Levels, AGENTS, original requests, both audit attachments, all 2368 plan lines and both Ledgers were reread before further task action. Authority Manifest 1.0.2 and all recorded SHA-256 values match; `npm.cmd run validate:authority` PASS and the P0 diff is empty. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; current dirty inventory is 62 collapsed / 66 expanded files = 50 tracked + 16 untracked and remains inside the recorded C3 owner/test/UI/contract/smoke write set. Fresh GitHub CURRENT is unchanged: main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs/issues, remote task branch absent, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. The original root remains `fd451c83005e444f84161e0c905f853a2b36d5a3` with 28 preserved dirty entries. Resume only the explicit C3 14.1-14.9 / ACC-C3-01..13 source-and-observable-effect matrix; push remains NOT_RUN.

Newest recovery: PASS at 2026-08-25 after the latest automatic compaction. The complete Constitution, Canonical Architecture, Capability Levels, AGENTS, both original attachments, all 2368 execution-plan lines and both Ledgers were reread before code work. The Authority Manifest remains 1.0.2; all five SHA-256 values match, `npm.cmd run validate:authority` PASS and P0 diff is empty. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; dirty inventory remains 62 collapsed / 66 expanded = 50 tracked + 16 untracked. GitHub CURRENT remains main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs/issues, remote task branch absent, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. Original root remains `fd451c83005e444f84161e0c905f853a2b36d5a3` with 28 preserved dirty entries. Continue only with the already-started C3 retry failure hardening and renderer action; push remains NOT_RUN.

Current recovery: PASS at 2026-08-25 01:10:21 +08:00 after the newest automatic compaction. The mandatory Constitution, Canonical Architecture, Capability Levels, AGENTS, original user requests, both audit attachments, all 2368 execution-plan lines and both Ledgers were replayed before task action. Authority Manifest 1.0.2 and every recorded SHA-256 match; `npm.cmd run validate:authority` PASS and P0 diff is empty. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; dirty inventory remains 62 collapsed / 66 expanded = 50 tracked + 16 untracked. Fresh GitHub CURRENT remains main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs/issues, remote task branch absent, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. The original root remains `fd451c83005e444f84161e0c905f853a2b36d5a3` with 28 preserved dirty entries. Resume only `ACC-C3-06_PRODUCT_CORPORATE_ACTION_OWNER_AUDIT`; no completed C3 work is repeated and push remains NOT_RUN.

Current recovery: PASS at 2026-08-25 01:31:17 +08:00 after the latest automatic compaction. The full Constitution, Canonical Architecture, Capability Levels, AGENTS, original user requests, both audit attachments, all 2368 execution-plan lines and both Ledgers were replayed before code work. Authority Manifest 1.0.2 and all five SHA-256 values match; `npm.cmd run validate:authority` PASS and P0 diff is empty. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; the bounded C3 inventory is now 63 collapsed / 67 expanded = 50 tracked + 17 untracked solely because the recorded new `product_corporate_actions.py` owner is present. GitHub CURRENT remains main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs/issues, remote task branch absent, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. The original dirty root remains preserved at `fd451c83005e444f84161e0c905f853a2b36d5a3`; 29 status entries are currently observed and none were touched by this task. Resume only `C3_14_5_RESULT_SERVICE_BINDING_AUDIT`; completed C3 work is not repeated and push remains NOT_RUN.

Current recovery: PASS at 2026-08-25 01:42:51 +08:00 after the current automatic compaction. The complete Constitution, Canonical Architecture, Capability Levels, AGENTS, original user requests, both audit attachments, all 2368 execution-plan lines and both Ledgers were replayed before task action. Authority Manifest 1.0.2 and all five SHA-256 values match; `npm.cmd run validate:authority` PASS and P0 diff is empty. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; the bounded C3 inventory remains 63 collapsed / 67 expanded = 50 tracked + 17 untracked. Fresh GitHub CURRENT remains main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs/issues, remote task branch absent, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. The original dirty root remains preserved at `fd451c83005e444f84161e0c905f853a2b36d5a3`; 28 current status entries were observed and none were touched by this task. Resume only the already-written ResultService 14.5 red acceptance, beginning with correction of its analytics wire-key expectation; no completed work is repeated and push remains NOT_RUN.

Current recovery: PASS at 2026-08-25 02:02:17 +08:00 after the latest automatic compaction. The complete Constitution, Canonical Architecture, Capability Levels, AGENTS, original user requests, both audit attachments, all 2368 execution-plan lines and both Ledgers were replayed before further task action. Authority Manifest 1.0.2 and all five SHA-256 values match; `npm.cmd run validate:authority` PASS and P0 diff is empty. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; the bounded C3 inventory is now 64 collapsed / 68 expanded = 50 tracked + 18 untracked because the planned `product_results.py` owner is present. Fresh GitHub CURRENT remains main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs/issues, remote task branch absent, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. Original dirty root remains `fd451c83005e444f84161e0c905f853a2b36d5a3` with 28 preserved status entries. Resume only the ResultService source/effect audit and full validation recorded at the top of this Ledger; push remains NOT_RUN.

Current recovery: PASS at 2026-08-25 02:22:15 +08:00 after automatic compaction during the second post-fix `validate:public`. The complete mandatory authority/task/attachment/2368-line plan/both-Ledger sequence was replayed; Authority 1.0.2 and all five SHA-256 values match, `npm.cmd run validate:authority` PASS and P0 diff is empty. Worktree branch/HEAD/tree/parent remain `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; bounded inventory is 65 collapsed / 69 expanded = 51 tracked + 18 untracked after excluding `.codex-tmp`. GitHub CURRENT remains main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs/issues, remote task branch absent; exact-main CI runs 32628893718 and 32626717592 plus packaging 32626717564 are SUCCESS. Original root remains `fd451c83005e444f84161e0c905f853a2b36d5a3` with 28 preserved entries. The vanished validation handle leaves the completed process result UNKNOWN; rerun it once for exact evidence, do not infer PASS, and do not push.

Current recovery: PASS at 2026-08-25 03:21:59 +08:00 after the latest automatic compaction. The complete Constitution, Canonical Architecture, Capability Levels, AGENTS, both original audit attachments, all 2368 execution-plan lines and this Ledger were reread before task action. Authority Manifest 1.0.2 and all five recorded SHA-256 values match; P0 diff is empty and P0 amendment authorization remains ABSENT. Worktree/branch/HEAD/tree/parent remain `D:\V3OpenSource-worktrees\v1-1-usable-research-product-01` / `codex/v1-1-usable-research-product-01` / `07cefa53e16ebed277a2bd1423f851379aa4732b` / `0a3656beeab164b420c27f7b251776bdab13327c` / `f19a18c8ad1901abd72f6f9e81b5b90de8f20fe6`; current bounded inventory is 67 collapsed / 71 expanded = 52 tracked + 19 untracked, including only the recorded temporary contract generator outside the eventual write set. Fresh GitHub CURRENT remains main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs/issues, remote task branch absent, exact-main CI 32626717592 SUCCESS and packaging 32626717564 SUCCESS. The original dirty root remains preserved at `fd451c83005e444f84161e0c905f853a2b36d5a3`; no root mutation was made. Resume only the recorded preview/export contract and desktop-stub synchronization; push remains NOT_RUN.

Current recovery: PASS at 2026-08-25 after the C4 packaged-run compaction. Priority goal/progress and the C4 effect matrix were recovered; the P0 Manifest is authority 1.0.2 and all five hashes match, P0 diff is empty, and amendment authorization is ABSENT. Worktree/branch/HEAD remain `D:\V3OpenSource-worktrees\v1-1-usable-research-product-01` / `codex/v1-1-usable-research-product-01` / `ffb441590e1efb442ec8c1c20f68e74004ae177c`. Fresh GitHub CURRENT is unchanged at main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, with zero open PRs and the remote task branch absent. Continue only by rebuilding and rerunning packaged Journey A after the recorded Task output fix; push remains NOT_RUN.

Current recovery: PASS at 2026-08-25 after the newest automatic compaction. The mandatory authority/task/plan/Ledger sequence was replayed and current identities were independently revalidated: Authority Manifest 1.0.2 hashes all match, P0 diff is empty, amendment authorization is ABSENT, and worktree/branch/HEAD/tree remain `D:\V3OpenSource-worktrees\v1-1-usable-research-product-01` / `codex/v1-1-usable-research-product-01` / `ffb441590e1efb442ec8c1c20f68e74004ae177c` / `b07ab828634bd71f8232d0265f12b9cf35eb4eb8`. Fresh GitHub CURRENT remains main/tree `02c5b8748170569ffc436f3bf5d2f682c21d2811` / `e3f3d3155177c17580015f4ef5b5405d0b689774`, zero open PRs, remote task branch absent. The bounded C4 inventory is 10 tracked plus 2 untracked files. Resume only adjacent worker/lifecycle validation, package rebuild, and packaged Journey A rerun after the terminal-child exit correction; push remains NOT_RUN.
