# Evidence & Artifact UX V0 — Reuse Adoption Matrix

Task: `V3-ROUND4-TRACK-N-EVIDENCE-ARTIFACT-UX-V0-01`
Research cutoff: 2026-08-12 (Asia/Shanghai)
Authority boundary: research and UI design input only. No candidate below becomes V3 canonical identity, Artifact Store, provenance, lineage, truth, admission, validation, session, or publication authority.

## Decision vocabulary

- `DIRECT_REUSE`: use an already-admitted dependency or current-main contract without copying its authority.
- `THIN_ADAPTER`: preserve the existing owner and expose a bounded read-only view.
- `DESIGN_REFERENCE`: borrow interaction or information-design ideas only; no runtime/code/data-store adoption.
- `REJECT`: do not use because it would create incompatible semantics, active-content risk, or a second authority.
- `V3_NATIVE_REQUIRED`: the behavior must be implemented as a V3-owned read-only view because no external system can satisfy the exact authority contract.

## Matrix

| Candidate | Revision / source checked | License / maintenance | Exact provenance semantics | Persistence / second-authority risk | Graph / Electron fit | Decision |
|---|---|---|---|---|---|---|
| Current V3 Artifact identity, descriptor, repository ports, and safe renderer registry | GitHub CURRENT `0d9799f0d47285d10246c23f9f1494105f20848a`; `domain/artifacts`, repository ports, `agentWorkspace.ts` | Repository Apache-2.0; current-main authority | Content-addressed artifact IDs and immutable descriptors are exact. The current renderer validates a closed passive payload shape, but does not hash rendered bytes. | Reusing is mandatory; a new store, file-path owner, or local graph database would be a second authority. | Native and already used by the Electron renderer. | `DIRECT_REUSE` |
| Round 3 canonical evidence bundle and lineage edges | GitHub CURRENT `0d9799f0d47285d10246c23f9f1494105f20848a`; `v3.round3_research_evidence_bundle/1.1.0` | Repository Apache-2.0; merged and exact-main CI passed | Parser rejects wrong object/hash pairs and any missing, extra, duplicate, or wrong exact edge. | No persistence is added. Track N consumes the parsed bundle and never rewrites it. | Small session-scoped graph is directly suitable. | `DIRECT_REUSE` |
| `@xyflow/react` already pinned by V3 | Local lock: `12.11.2`, npm integrity `sha512-eLAl...ld12cA==`; upstream `xyflow/xyflow@e90a1b6acd63dded683ed0d3c24bf712716d4661` | MIT; active as of 2026-08-11. [Official repository](https://github.com/xyflow/xyflow) | Rendering library has no provenance semantics and is never treated as an edge source. | None when fed an in-memory V3 view model only. | Already shipped in Electron. Official guidance recommends memoization, collapsed large trees, simple styling, and bounded/visible rendering. [Performance](https://reactflow.dev/learn/advanced-use/performance), [`ReactFlow` API](https://reactflow.dev/api-reference/react-flow) | `DIRECT_REUSE` |
| Current Evidence Inspector and Artifact Viewer | GitHub CURRENT `0d9799f0d47285d10246c23f9f1494105f20848a` | Repository Apache-2.0 | Preserves exact IDs and safe renderer shapes, but currently drops Round 3 edges and lacks explicit integrity/discovery state. | Keep the existing mount, types, safe renderer registry, and five-Lab navigation; add no persistence. | Native Electron/React surface. | `THIN_ADAPTER` |
| OpenBB Workspace citations/provenance UX | OpenBB core `3e071fcc2cd9f891cac6040ae60296dba76dab46`; [citation highlighting](https://docs.openbb.co/workspace/developers/ai-features/highlight-widget-citations), [output citations](https://docs.openbb.co/workspace/analysts/ai-features/copilot-output) | OpenBB repository API reports `NOASSERTION`; Workspace UI/code license was not established from the reviewed docs. Maintained July 2026. | Useful pattern: citations point back to the exact widget/source and support direct navigation. It does not define V3 object/hash, truth, or admission semantics. | Importing Workspace persistence or generated widgets would create foreign authority and active-content concerns. | Interaction reference only. | `DESIGN_REFERENCE` |
| MLflow runs and artifacts UX | `mlflow/mlflow@3128eb46928fab08500124e9e0cde6791cf845aa`, release `v3.15.1`; [artifact stores](https://mlflow.org/docs/latest/self-hosting/architecture/artifact-store/), [tracking API](https://www.mlflow.org/docs/latest/ml/tracking/tracking-api) | Apache-2.0; active as of 2026-08-11 | Explicit run IDs, nested parent IDs, artifact URIs, and run inputs/outputs are useful navigation concepts. URIs and MLflow run metadata are not V3 exact artifact identity or formal provenance admission. | MLflow brings its own backend metadata store and artifact store; adopting either would create a second authority. | Run/artifact detail and parent-child navigation are useful references; no runtime adoption. | `DESIGN_REFERENCE` |
| W&B Artifacts lineage UX | `wandb/wandb@73edebbdd2fc88eb668dd2199eadeb46f42e089a`, release `v0.28.1`; [artifact graph](https://docs.wandb.ai/models/artifacts/explore-and-traverse-an-artifact-graph) | MIT; active as of 2026-08-11 | `use_artifact` and `log_artifact` create explicit input/output links; `used_by()` is an exact reverse traversal. This supports the V3 rule that `used-by` must be a reverse index of known edges. | W&B artifact/run storage and registry are separate authorities and are not adopted. | Focus, hide/show, clusters, detail preview, and upstream/downstream traversal are strong UI references. | `DESIGN_REFERENCE` |
| DVC pipeline lineage concepts | `treeverse/dvc@56e59829512ff134aa269099a2099587b810b4dd`, release `3.67.1`; [`dvc dag`](https://dvc.org/doc/command-reference/dag), [pipeline definitions](https://dvc.org/doc/user-guide/pipelines/defining-pipelines) | Apache-2.0; active as of 2026-08-10 | Edges come from declared stage dependencies/outputs; target-scoped ancestor view and collapsed stages are useful bounded-graph concepts. File dependencies are not V3 canonical financial/object provenance. | DVC YAML/lock/cache would be a parallel pipeline/artifact authority. | Ancestor focus and collapsed graph are design references only. | `DESIGN_REFERENCE` |
| OpenLineage concepts and event schema | `OpenLineage/OpenLineage@7b7cf864fcbb1d45858eb93fc402df4b430d369c`, release `1.52.0`; [API](https://openlineage.io/apidocs/openapi/), [facets](https://openlineage.io/docs/spec/facets/) | Apache-2.0; active as of 2026-08-11 | Explicit Run/Job/input/output Dataset relations and producer/schema versioning are compatible concepts, but generic namespaces and accumulative events do not prove V3 exact object/hash bindings or completeness. | An OpenLineage backend/event collector would become another persisted discovery authority. | Relation naming and producer/binding detail are references; no SDK/backend adoption. | `DESIGN_REFERENCE` |
| Similarity-, AI-, label-, timestamp-, symbol-, or ID/hash-prefix inferred edges | Not applicable | Not applicable | These are not explicit relations and cannot satisfy the source boundary. | Would silently invent graph authority. | Misleading regardless of renderer. | `REJECT` |
| Track N exact graph projection, session scope, integrity/discovery vocabulary, filters, and Lab routing | V3 CURRENT contracts plus Track N-owned source | Repository Apache-2.0 | Must validate endpoint IDs and full hashes, preserve the binding ref, and derive `used-by` only by reversing accepted exact edges. Missing means `NO_KNOWN_RELATION`; incomplete discovery means `DISCOVERY_SCOPE_LIMITED`. | Pure in-memory UI view; no persistence, store, global index, or ID factory. | Purpose-built for current Electron, five Labs, and bounded React Flow rendering. | `V3_NATIVE_REQUIRED` |

## Adoption outcome

The implementation will directly reuse the merged Round 3 parser/edge set, current Artifact/safe-renderer contracts, and pinned React Flow. It will add a Track N-owned in-memory projection only. W&B, DVC, MLflow, OpenLineage, and OpenBB supply navigation/information-design references; none is installed, queried at runtime, persisted, or allowed to author an edge.

The production source is currently connected but session-bounded and not a proven global discovery owner. The explorer therefore reports `DISCOVERY_SCOPE_LIMITED` even when a local query has no results. Artifact byte verification is not exposed to the renderer; the UI reports `NOT_RUN`/`UNKNOWN` rather than inferring integrity from a file path, renderer validation, or file existence.

## PR #22 authority closure

- Discovery provenance is supplied by the actual `AgentWorkspaceBoundary`. Current-main canonical projection, development/integration fixture, connected-empty, disconnected, and unknown inputs remain distinct closed source values. Expanding from active session to loaded workspace changes visibility only and never relabels the upstream source.
- `EXPLICIT_ARTIFACT_REF` means only an exact reference from the loaded Evidence/Artifact view with full endpoint hashes. Relation presence does not assert byte integrity.
- A distinct Artifact node never inherits Truth, Admission, Validation, integrity, or reviewer status from its source Evidence. Without Artifact-owned authority, Artifact Truth/Admission are `UNKNOWN`, Artifact Validation is `NOT_RUN`, and byte Integrity remains `NOT_RUN`.
- Source Evidence object, Truth, Admission, and Validation remain separately visible as source authority metadata. They are not displayed as Artifact-owned state.

## Research limitation

The repository instruction prefers Context7 for library documentation. No Context7 tool was available in this session, so current official documentation and official repository metadata were used instead. No non-official implementation guide was used as authority.
