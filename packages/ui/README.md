# Shared UI foundations

The recovered application keeps the compact workbench visual system in `apps/desktop/src/renderer/styles.css` so the Electron shell can load it without a runtime package boundary. The visual foundations are documented and intentionally small: dark neutral canvas, cyan active state, persistent navigation rail, contextual inspector, and truthful availability banners.

