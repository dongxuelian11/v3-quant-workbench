# Desktop application

Electron main, preload, and renderer sources for the recovered continuous V3 workbench. The renderer is intentionally backend-independent and uses typed IPC for desktop persistence and availability status.

