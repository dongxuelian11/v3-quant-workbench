# Narrow V1 free-source candidate: actual network use is still PRE_ALPHA / RESEARCH_ONLY / APPROXIMATE.
akshare==1.18.84
# AKShare 1.18.84 resolves this pure-Python dependency from its published source distribution.
jsonpath==0.82.2

# Track D uses only the provider-agnostic PydanticAI core. Provider SDKs and
# orchestration extras are intentionally excluded from the V0 authority boundary.
pydantic-ai-slim==2.27.0

# Windows has no system IANA timezone database; the packaged backend uses
# zoneinfo (for example Asia/Shanghai) during Product Runtime bootstrap.
tzdata==2025.3

# Track E process-isolated deterministic model worker.
-r requirements-model-runtime-v0.txt
